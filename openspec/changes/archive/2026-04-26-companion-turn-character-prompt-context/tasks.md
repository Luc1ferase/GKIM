# Tasks — companion-turn-character-prompt-context

## §1 — OpenSpec scaffold

- [x] §1.1 — `proposal.md` / `tasks.md` / `specs/llm-text-companion-chat/spec.md` delta committed on branch `feature/companion-turn-character-prompt-context` off `feature/ai-companion-im` (HEAD `30eddaf`). Scaffold commit `6edc786`, pushed to `origin/feature/companion-turn-character-prompt-context`. `openspec validate companion-turn-character-prompt-context --strict` returns valid. Paired backend slice `companion-turn-backend-llm-bridge` lives at `669c927` on `origin/feature/companion-turn-backend-llm-bridge` in `GKIM-Backend`.

## §2 — DTO surface

- [x] §2.1 — Add `@Serializable data class CharacterPromptContextDto(systemPrompt: String, personality: String, scenario: String, exampleDialogue: String, userPersonaName: String, companionDisplayName: String)` to `data/remote/im/ImBackendModels.kt` immediately above `CompanionTurnSubmitRequestDto`. No mapper to a domain type — the DTO carries already-resolved strings only. Verification: compiles (`:app:compileDebugKotlin` BUILD SUCCESSFUL); round-trip assertion deferred to §2.3's serialization test which exercises the full DTO graph against on-disk fixtures. (`d5b9fd9`)

- [x] §2.2 — Extend `CompanionTurnSubmitRequestDto`, `EditUserTurnRequestDto`, `RegenerateAtRequestDto` each with `val characterPromptContext: CharacterPromptContextDto? = null` as the last constructor parameter. The default `null` keeps every existing call-site source-compatible. Verification: `:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` BUILD SUCCESSFUL with zero existing call-site changes; old serialized payloads (without the field) still deserialize because `null` is the default. (`81b8645`)

- [x] §2.3 — Add `contract/fixtures/companion-turns/submit-request-with-character-context.json` and `submit-request-without-character-context.json` byte-mirroring the paired backend repo's fixtures. Add a new `feature` test `data/remote/im/CompanionTurnSubmitRequestDtoSerializationTest.kt` that round-trips both fixtures through the Kotlin DTO via the project's `Json` instance and asserts (a) field-present fixture deserializes with all six `CharacterPromptContextDto` fields populated, (b) field-absent fixture deserializes with `characterPromptContext == null`, (c) re-serializing the field-absent DTO produces JSON without the `characterPromptContext` key (kotlinx.serialization's encode-default behavior with `encodeDefaults = false`). Verification: `:app:testDebugUnitTest --tests "com.gkim.im.android.data.remote.im.CompanionTurnSubmitRequestDtoSerializationTest"` BUILD SUCCESSFUL (4 tests, all green); `git diff --no-index contract/fixtures/companion-turns/ ../GKIM-Backend/contract/fixtures/companion-turns/` empty for the two new files. Client commit `271e6ce`; backend mirror commit `5f4089a` (paired-PR landing).

## §3 — Resolution helper

- [x] §3.1 — New file `feature/chat/CharacterPromptContextResolver.kt` exposing `internal fun resolveCharacterPromptContext(card: CompanionCharacterCard?, persona: UserPersona?, language: AppLanguage): CharacterPromptContextDto?` and a private `defaultPersonaName(language: AppLanguage): String` (returns `"User"` for English, `"用户"` for Chinese). The helper:
  - Returns `null` when `card == null` so callers can bail before the wire call.
  - Otherwise computes `resolved = card.resolve(language)` and packs the six fields; uses `?.takeIf { it.isNotBlank() }` on the persona name so a blank-after-trim display name also falls through to the localized default.
  - Does NOT macro-substitute. The backend prompt-assembly module is the single substitution point per the paired backend slice's `design.md` §2.

  Verification: `:app:testDebugUnitTest --tests "com.gkim.im.android.feature.chat.CharacterPromptContextResolverTest"` BUILD SUCCESSFUL (5 tests green): null-card, full card + persona with macros literal, null persona EN default, null persona ZH default + Chinese-branch projection, blank persona name fallback. (`03283ad`)

## §4 — Repository surface

- [x] §4.1 — Extend `CompanionTurnRepository` trait methods with `characterPromptContext: CharacterPromptContextDto? = null`. All three default to `null`, preserving every existing internal call-site. `DefaultCompanionTurnRepository` keeps the default-throw on the trait method bodies (the live impl is the only path that consumes the parameter; the offline default-throw signals "no live wire" the same way it did pre-slice). Three test fakes (`ChatViewModelCompanionDispatchTest.RecordingCompanionTurnRepository`, `ChatViewModelEditUserTurnTest.RecordingCompanionTurnRepository`, `ChatViewModelRegenerateFromHereTest.RecordingCompanionTurnRepository`) updated with the new override signature + capture field so test compilation tracks the trait change. Verification: `:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` BUILD SUCCESSFUL. (`8136b5d`)

- [x] §4.2 — `LiveCompanionTurnRepository.submitUserTurn`, `.editUserTurn`, `.regenerateCompanionTurnAtTarget` each thread `characterPromptContext` into the corresponding outbound DTO (`CompanionTurnSubmitRequestDto`, `EditUserTurnRequestDto`, `RegenerateAtRequestDto`). The retry path in `retrySubmitUserTurn` replays the captured ctx from `FailedCompanionSubmission` byte-equivalently — `FailedCompanionSubmission` gains a `characterPromptContext: CharacterPromptContextDto? = null` field. Verification: `:app:testDebugUnitTest --tests LiveCompanionTurnRepository*` BUILD SUCCESSFUL. Six new assertions across three suites: `LiveCompanionTurnRepositoryTest` adds submit-forwards-ctx, submit-defaults-null, retry-replays-ctx; `LiveCompanionTurnRepositoryEditUserTurnTest` adds edit-forwards-ctx + edit-defaults-null; `LiveCompanionTurnRepositoryRegenerateAtTest` adds regenAt-forwards-ctx + regenAt-defaults-null. Each forwarded-ctx test uses the existing FakeBackendClient captured-DTO assertion to lock the wire shape. (`8136b5d`)

## §5 — Call-site wiring

- [x] §5.1 — `ChatViewModel` constructor now accepts `companionRosterRepository: CompanionRosterRepository`; `userPersonaRepository` is stored as a field and an init-block coroutine collects `observeActivePersona()` into a private `MutableStateFlow<UserPersona?>`. New private helper `resolveActiveCardPromptContext(companionId, language)` looks up the card via `companionRosterRepository.characterById(...)` and delegates to `resolveCharacterPromptContext`. `sendMessage` calls the helper on the companion-card branch and threads the result into `companionTurnRepository.submitUserTurn(...)`. Verification: `ChatViewModelCompanionDispatchTest` gains `sendMessage forwards resolved characterPromptContext when an active card and persona are in scope` (asserts all six DTO fields with macros un-substituted) plus `sendMessage forwards null characterPromptContext when no card is registered`. (`ed57bd4`)

- [x] §5.2 — `ChatViewModel.editUserTurn` resolves ctx via the same helper using `draftSheet.activeCompanionId` before launching the dispatch. `regenerateFromHere` gains an optional `activeLanguage: AppLanguage` parameter (route passes `LocalAppLanguage.current`) and resolves ctx using the conversation's `companionCardId`. The route's `onRegenerateFromHere` delegates through a lambda so `appLanguage` reaches the ViewModel. Verification: `:app:testDebugUnitTest` full sweep BUILD SUCCESSFUL — `ChatViewModelEditUserTurnTest` + `ChatViewModelRegenerateFromHereTest` both compile against the new constructor signature (six total test sites updated to pass `companionRosterRepository = stubCompanionRosterRepository()` via the new helper in `ChatViewModelTestStubs.kt`); no regressions across the whole `:app` test suite. (`ed57bd4`)

## §6 — Verification + archive

- [x] §6.1 — Verification roll-up: §1-§5 evidence rows recorded in `docs/DELIVERY_WORKFLOW.md` under `companion-turn-character-prompt-context delivery evidence`. All new unit tests green: `CompanionTurnSubmitRequestDtoSerializationTest` (4), `CharacterPromptContextResolverTest` (5), `LiveCompanionTurnRepositoryTest` (3 new), `LiveCompanionTurnRepositoryEditUserTurnTest` (2 new), `LiveCompanionTurnRepositoryRegenerateAtTest` (2 new), `ChatViewModelCompanionDispatchTest` (2 new). `:app:testDebugUnitTest` full sweep BUILD SUCCESSFUL with no regressions. Paired backend slice `companion-turn-backend-llm-bridge` exists at `669c927` (scaffold) + `5f4089a` (fixture mirror) on `origin/feature/companion-turn-backend-llm-bridge` in `GKIM-Backend`; client is forward-compatible against the deployed S1 backend (`Option<CharacterPromptContext>` with `serde(default)` accepts the new field). Evidence commit `cd5b010`.

- [x] §6.2 — Archive — moved `openspec/changes/companion-turn-character-prompt-context` → `openspec/changes/archive/2026-04-26-companion-turn-character-prompt-context/` and applied the spec delta to `openspec/specs/llm-text-companion-chat/spec.md` (2 ADDED Requirements). Verification: `openspec archive companion-turn-character-prompt-context --yes` reported `+ 2 added` to `llm-text-companion-chat`; post-archive `openspec validate --specs --strict` returns `16 passed, 0 failed`.
