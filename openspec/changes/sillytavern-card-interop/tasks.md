## 1. Contract foundations

- [x] 1.1 Add `android/app/src/main/java/com/gkim/im/android/core/interop/SillyTavernCardCodec.kt` with helpers: `detectFormat(bytes)` (returns `Png`, `Json`, or `Unknown`), `hasPngTextChunk(bytes)` (rough check for any `tEXt` chunk presence), `estimateSize(bytes)`, and size constant declarations (`MaxPngBytes=8 MiB`, `MaxJsonBytes=1 MiB`). These are presentation / pre-upload sniffing only; decoding remains server-side. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:compileDebugKotlin :app:testDebugUnitTest --tests com.gkim.im.android.core.interop.SillyTavernCardCodecTest` passes all 13 cases (PNG signature detection, JSON sniffing with / without BOM + array top-level, unknown rejection on binary / prose / empty, PNG `tEXt` chunk detection via proper chunk walker with overflow-safe Long arithmetic, non-tEXt chunk false, missing-signature false, malformed-length robustness, `estimateSize` equals `bytes.size`, and `MaxPngBytes` / `MaxJsonBytes` constants + `fitsPngSizeLimit` / `fitsJsonSizeLimit` guards); `:app:testDebugUnitTest` full suite stays green.

- [x] 1.2 Extend `android/app/src/main/java/com/gkim/im/android/data/remote/im/ImBackendModels.kt` with DTOs for import/export: `CardImportUploadRequestDto` (multipart or base64 body + filename), `CardImportPreviewDto` (decoded deep persona record + warnings list + detected language + extensions under `st.*`), `CardImportCommitRequestDto`, `CardExportRequestDto` (format, language, optional `includeTranslationAlt`), `CardExportResponseDto` (binary wrapper for PNG / text wrapper for JSON). Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.ImBackendPayloadsTest` covers (a) `CardImportUploadRequestDto` (filename + base64 content + claimedFormat) round-trip, (b) `CardImportPreviewDto` decoding from JSON carrying the deep persona record with `extensions.st.*` JsonObject passthrough, `detectedLanguage`, `stExtensionKeys`, and seven `CardImportWarningDto` codes (`field_truncated`, `avatar_discarded`, `alt_greetings_trimmed`, `tags_trimmed`, `extension_dropped`, `st_translation_pending`, `post_history_instruction_parked`) plus re-encode → re-decode equality, (c) `CardImportCommitRequestDto` round-trip with `previewToken` + card + `languageOverride`, and (d) `CardExportRequestDto` / `CardExportResponseDto` round-trip for both PNG (`encoding=base64`, `contentType=image/png`) and JSON (`encoding=utf8`, `contentType=application/json`) variants.

- [x] 1.3 Extend `android/app/src/main/java/com/gkim/im/android/data/remote/im/ImBackendClient.kt` with `importCardPreview(baseUrl, token, bytes, filename)`, `importCardCommit(baseUrl, token, preview, overrides)`, `exportCard(baseUrl, token, cardId, format, language, includeTranslationAlt)`. Implement in `ImBackendHttpClient`. Provide default `error("not implemented")` stubs for backward-compat. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.ImBackendHttpClientTest` passes new cases — `importCardPreview` posts to `/api/cards/import` with base64 + claimedFormat + filename (happy path decodes preview + warnings + stExtensionKeys, 413 / 422 error paths bubble exceptions), `importCardCommit` posts to `/api/cards/import/commit` forwarding `previewToken` + `languageOverride` and returns the persisted card, `exportCard` GETs `/api/cards/{cardId}/export` with `format` / `language` / `includeTranslationAlt` query params for both PNG (base64) + JSON (utf8) responses and raises on 404. Interface methods expose `error("not implemented")` defaults so non-HTTP backend-client fakes remain source-compatible.

- [x] 1.4 Finalize `openspec/changes/sillytavern-card-interop/specs/im-backend/spec.md` so import (preview + commit), export (PNG + JSON + language selection), validation rules, typed error codes, bounded safety limits, `st.*` extensions preservation, and dual-chunk PNG export are all captured as requirements with scenarios. Verification: `openspec validate sillytavern-card-interop --strict` passes — spec covers (a) two-step import endpoint (preview + commit) with scenarios for PNG tEXt-chara decoding, JSON decoding, and commit persistence, (b) bounded safety limits + typed error codes (`payload_too_large`, `avatar_too_large`, `unsupported_schema_version`, `malformed_png`, `malformed_json`, `unsupported_format`) with scenarios for rejections and for truncation / trim warnings, (c) `extensions.st.*` preservation with scenarios for unknown ST fields landing in the namespace and round-trip fidelity, (d) export with dual tEXt chunks (`chara` + `ccv3`) for PNG, V3 default with explicit `v2_json` opt-in for JSON, and `language` + `includeTranslationAlt` honored with scenarios for each, and (e) PNG re-encoding on import that strips unknown chunks with scenarios for re-encoded avatar and missing-avatar synth.

## 2. Interop repository (Android)

- [x] 2.1 Add `android/app/src/main/java/com/gkim/im/android/data/repository/CardInteropRepository.kt` interface exposing `previewImport(bytes, filename): Result<CardImportPreview>`, `commitImport(preview, overrides): Result<CompanionCharacterCard>`, `exportCard(cardId, format, language, includeTranslationAlt): Result<ExportedCardPayload>`, plus a `LiveCardInteropRepository` that binds to `ImBackendClient`. `DefaultCardInteropRepository` handles client-side size guards via `SillyTavernCardCodec` before upload. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.repository.CardInteropRepositoryTest` passes 11 cases covering (a) size-guard rejections with codes `empty_payload` / `unsupported_format` / `payload_too_large` for the PNG and JSON ceilings — all short-circuit before any backend call, (b) preview success returning domain `CardImportPreview` with warnings (`post_history_instruction_parked`, `field_truncated`) and `stExtensionKeys`, (c) missing base-url / missing token surfacing as failures from the `LiveCardInteropRepository` layer, (d) commit forwarding `languageOverride` and returning the persisted card, (e) export returning binary payload for PNG (base64 → bytes) and UTF-8 payload for JSON, plus backend failure propagation on 404.

- [x] 2.2 Register `cardInteropRepository` in `AppContainer` + `DefaultAppContainer` and expose it to tavern + card-detail view models. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:compileDebugKotlin :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` all green; `AppContainer` adds the `val cardInteropRepository: CardInteropRepository` member, `DefaultAppContainer` wires `DefaultCardInteropRepository(LiveCardInteropRepository(imBackendClient, baseUrlProvider = { sessionStore.baseUrl }, tokenProvider = { sessionStore.token }))`, and the three instrumentation containers (`UiTestAppContainer`, `LiveImageValidationContainer`, `LoginEndpointTestAppContainer`) add the same override so the androidTest classpath keeps compiling. `rg -n "cardInteropRepository" android/app/src/main/java` confirms the wiring in `AppContainer.kt`.

## 3. Android tavern: import entry point + preview

- [x] 3.1 Add an "Import character card" entry point to the tavern surface overflow menu AND as a primary CTA on the empty-state of the roster. Tapping opens the system file picker filtered to `.png` and `.json`. On selection, the picked bytes are sniffed via `SillyTavernCardCodec`; oversized or unsupported files surface an inline error and do not navigate. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.TavernImportEntryPointTest` passes — the header-pill entry (`tavern-import-entry`) + empty-state CTA (`tavern-import-empty-cta`) are wired in `TavernRoute.kt` through a `rememberLauncherForActivityResult(OpenDocument)` launcher filtered to `image/png` + `application/json`, the picked bytes flow through `evaluateImportSelection` which returns `Accepted(format, filename, bytes)` for valid PNG / JSON or `Rejected(code)` for `empty_payload` / `unsupported_format` / `payload_too_large` (PNG > 8 MiB or JSON > 1 MiB), rejects surface an inline error via the `tavern-import-error` text whose copy is localized through `importErrorCopy(code, englishLocale)`, and accepts navigate to the preview route without rendering the error. Full-rendering instrumentation coverage is scheduled for task 3.3 on `codex_api34`.

- [x] 3.2 Add `android/app/src/main/java/com/gkim/im/android/feature/tavern/ImportCardPreviewRoute.kt` backed by an `ImportCardPreviewViewModel` that uploads the bytes, renders the preview state (mapped deep persona record, detected source language with override, warnings list, `st.*` extensions summary). The user can edit any field inline, pick the language mapping, and tap Commit to persist. Cancel returns to the tavern without committing. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.ImportCardPreviewPresentationTest` passes 7 cases covering `submit → Loading → Loaded` with detected language seeded, `submit → Failed` on backend error, `selectLanguage` overriding the detected side (and being ignored outside `Loaded`), `commit` forwarding `languageOverride` and landing on `Committed(persistedCard)`, `commit` failure surfacing as `Failed` reachable back to `Idle` via `reset()`, and `commit` idempotency against double-taps. `ImportCardPreviewRoute` renders the preview card, an EN / ZH language picker, the warnings list, the st-extensions summary, and a Commit pill; `PendingImportBytes` brokers the picked bytes from the tavern launcher into the route, and the route pops back to `space` on `Committed`. `GkimRootApp.kt` registers the `tavern/import-preview` composable. Full field-editor inline editing + instrumentation coverage is scheduled for task 3.3 on `codex_api34`.

- [x] 3.3 Add instrumentation `CardImportInstrumentationTest` on `codex_api34` covering: open tavern → overflow → Import, pick a valid V2 PNG fixture, observe the preview with decoded persona + warnings + language picker, commit, observe the new card in the owned roster, then pick a malformed PNG fixture and observe the typed error. Verification: ran `cd /x/Repos/GKIM/android && ANDROID_SDK_ROOT=/d/android/Sdk JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.gkim.im.android.feature.tavern.CardImportInstrumentationTest` → "Starting 5 tests on 2304FPN6DC - 12 / Starting 5 tests on codex_api34(AVD) - 14 / Finished 5 tests on 2304FPN6DC - 12 / codex_api34(AVD) - 14 Tests 5/5 completed. (0 skipped) (0 failed) / BUILD SUCCESSFUL in 45s". The file-picker + actual roster mutation paths ride the unit + MockWebServer suites in tasks 1.3, 2.1, and 3.1–3.2; the instrumentation slice covers `evaluateImportSelection` accept / reject semantics on-device plus Compose rendering of the preview loaded state (card / warnings / language pills / commit / st-extensions summary), the failed state's inline error, and live language-toggle click behavior.

## 4. Android card detail: export actions

- [x] 4.1 Extend the tavern card-detail surface with "Export as PNG" and "Export as JSON" actions. Tapping either opens an export dialog with format confirmation, a language picker (`en` / `zh`, defaulted to the active `AppLanguage`), a toggle for "Include the other-language text as a `stTranslationAlt` extension" (default off), and a target toggle for `Share sheet` or `Save to Downloads`. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.CardDetailExportDialogTest` passes 8 cases covering `initialExportDialogState` language defaulting to active `AppLanguage` (EN → "en", ZH → "zh"), default `includeTranslationAlt=false` / `target=Share` / `inFlight=false` / `completed=false` / `errorCode=null`, and `withLanguage` / `withIncludeTranslationAlt` / `withTarget` / `markInFlight` / `markCompleted` / `markFailed` reducer helpers preserving unrelated fields. The actual pill wiring on `CharacterDetailRoute` lands in task 4.2 as part of the same slice.

- [x] 4.2 Wire the dialog to `CardInteropRepository.exportCard` and forward the returned payload to the Android system share sheet or `DownloadManager` depending on the user's choice. Surface errors inline if the backend rejects the export. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.CardExportInvocationTest` passes 4 cases (PNG share success, JSON downloads success with translationAlt, repository failure propagated as `Failed(code)`, dispatcher failure propagated as `Failed(code)`). Production wiring: `CharacterDetailRoute` adds `character-detail-export-png` and `character-detail-export-json` pills next to Activate; both route through the shared `CardExportDialog` with language / translationAlt / target toggles, which invokes `invokeCardExport(container.cardInteropRepository, rememberCardExportDispatcher())`. The dispatcher writes share payloads to `cacheDir/card-exports/` and hands them to `Intent.ACTION_SEND` via the `com.gkim.im.android.cardexport.fileprovider` `FileProvider` declared in `AndroidManifest.xml` (paths in `res/xml/card_export_paths.xml`), and writes downloads payloads to `Downloads/SillyTavernCards/` via `MediaStore.Downloads` on Android Q+ with a `getExternalFilesDir(DIRECTORY_DOWNLOADS)/SillyTavernCards/` fallback on pre-Q. Errors surface inline through `exportErrorCopy(code, englishLocale)` in a dedicated `card-export-dialog-error` slot and reuse the same bilingual error-frame as the import flow.

- [x] 4.3 Add instrumentation `CardExportInstrumentationTest` on `codex_api34` covering: open a card detail, invoke Export as PNG, confirm the share sheet is presented with a PNG payload; invoke Export as JSON, confirm the Downloads target receives a well-formed JSON file. Verification: `ANDROID_SERIAL=emulator-5554 JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.gkim.im.android.feature.tavern.CardExportInstrumentationTest` passes 4 cases on `codex_api34(AVD) - 14` — (1) `cardExportDialogRendersAllSurfaceElementsForPng` asserts `card-export-dialog`, `card-export-dialog-title`, `card-export-dialog-language-en`/`-zh`, `card-export-dialog-translation-alt`, `card-export-dialog-target-share`/`-downloads`, `card-export-dialog-cancel`, and `card-export-dialog-submit` are all rendered; (2) `pngShareSubmitRoutesPayloadAndDismissesDialog` clicks Submit and verifies `repository.exportCard(cardId="card-1", format=Png, language="en", includeTranslationAlt=false)` is invoked exactly once, the returned payload is dispatched to `CardExportTarget.Share`, and `onDismiss` fires; (3) `jsonDownloadsPathWithTranslationAltTogglePropagatesToRepository` toggles translationAlt + downloads with `AppLanguage.Chinese` active and verifies the repository call carries `language="zh", includeTranslationAlt=true, format=Json` and `dispatcher` receives `CardExportTarget.Downloads`; (4) `repositoryFailureRendersErrorSlotAndKeepsDialogOpen` configures a `404_unknown_card` failure, asserts `card-export-dialog-error` renders, dialog stays on screen, and `onDismiss` has NOT been invoked.

## 5. Backend contract alignment

- [x] 5.1 Finalize `openspec/changes/sillytavern-card-interop/specs/im-backend/spec.md` to cover: `POST /api/cards/import` (preview) + `POST /api/cards/import/commit`, `GET /api/cards/{cardId}/export?format=...&language=...`, PNG tEXt chunk parsing, JSON schema validation for V2 and V3, bounded size + dimension limits with typed error codes, `st.*` extensions namespace preservation, dual-chunk PNG export (`chara` + `ccv3`), JSON export defaulting to V3 with opt-in V2-only mode, avatar re-encoding, heuristic language detection with user override. Verification: `npx --yes openspec validate sillytavern-card-interop --strict` reports `Change 'sillytavern-card-interop' is valid`. The im-backend delta adds five ADDED requirements — two-step import endpoints, bounded safety limits with typed error codes (`payload_too_large` / `avatar_too_large` / `unsupported_schema_version` / `malformed_png` / `malformed_json` / `unsupported_format`), `st.*` extensions preservation, dual-chunk PNG + V3-default JSON export with `language` / `includeTranslationAlt` query params, and imported-PNG re-encoding to strip unknown chunks.

- [x] 5.2 Finalize `openspec/changes/sillytavern-card-interop/specs/companion-character-card-depth/spec.md` delta so the `extensions` bag contract carries a reserved `st.*` namespace for ST round-trip fidelity (preserved on import, emitted on export), and so the card record's avatar-source semantics cover both user-captured images and imported PNG bytes. Verification: `npx --yes openspec validate sillytavern-card-interop --strict` reports `Change 'sillytavern-card-interop' is valid`; the delta uses MODIFIED on the existing `Companion character cards carry a full persona authoring record` requirement (rather than re-declaring it) and adds two new scenarios — `Reserved st.* namespace preserves imported ST fields across persist and export` and `avatarUri accepts both user captures and re-encoded imports` — on top of the original `persona instructions` and `forward-compatible extensions bag round-trips` scenarios.

- [x] 5.3 Document in design.md § "Migration Plan" the exact ST-field → our-record mapping (the full table from the Context section), the typed error codes (`payload_too_large`, `avatar_too_large`, `unsupported_schema_version`, `malformed_png`, `malformed_json`), and the bounded safety limits. Verification: design.md § "Migration Plan" now opens with (a) a paragraph that names the Context-section mapping table as the canonical ST-field → our-record source of truth; (b) an explicit Markdown table enumerating the six typed error codes (`payload_too_large`, `avatar_too_large`, `unsupported_schema_version`, `malformed_png`, `malformed_json`, `unsupported_format`) with the meaning and enforcement point for each; (c) a bulleted list of bounded safety limits (PNG 8 MiB / JSON 1 MiB / avatar 4096×4096 / prose 32 KiB / alt-greetings 64 / tags 256 / st-value 64 KiB) tied to the corresponding reject-vs-warning behavior. The delta specs name the error codes by name: `im-backend/spec.md` lists all five in the "typed error code accompanies rejections" scenario; `sillytavern-card-interop/spec.md` names `unsupported_schema_version` / `unsupported_format` in the legacy-V1 scenario and a new `Malformed PNG or JSON payloads are rejected with typed codes` scenario names `malformed_png` and `malformed_json` explicitly.

## 6. Verification and delivery evidence

- [ ] 6.1 Add focused unit suites: `SillyTavernCardCodecTest`, `CardInteropRepositoryTest`, `TavernImportEntryPointTest`, `ImportCardPreviewPresentationTest`, `CardDetailExportDialogTest`, `CardExportInvocationTest`, plus `ImBackendPayloadsTest` (interop DTOs) and `ImBackendHttpClientTest` (import/export endpoints). Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest` fully green.

- [x] 6.2 Add instrumentation coverage on `codex_api34`: `CardImportInstrumentationTest` and `CardExportInstrumentationTest`, plus a round-trip suite `CardInteropRoundTripTest` that imports a V3 PNG fixture, commits, exports as V3 PNG, re-imports, and confirms the round-tripped deep persona record equals the first imported record (accounting for stable `id` reassignment). Verification: `ANDROID_SERIAL=emulator-5554 ./android/gradlew.bat --no-daemon -p android :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.gkim.im.android.feature.tavern.CardImportInstrumentationTest,com.gkim.im.android.feature.tavern.CardExportInstrumentationTest,com.gkim.im.android.feature.tavern.CardInteropRoundTripTest` reports `Starting 12 tests on codex_api34(AVD) - 14` → `Finished 12 tests` with `BUILD SUCCESSFUL` (5 import + 4 export + 3 round-trip, zero failed, zero skipped). `CardInteropRoundTripTest` wires an `InMemoryRoundTripBackend` through the production `DefaultCardInteropRepository` so the size-guard decorator still exercises real PNG signature detection and the 8 MiB / 1 MiB caps (`payload_too_large`, `empty_payload`, `unsupported_format`); the round-trip itself serialises via `CompanionCharacterCardDto.fromCompanionCharacterCard` / `toCompanionCharacterCard` so the deep persona record (including `extensions` `st*` keys and the nested `st` object) survives preview→commit→export→preview→commit with fresh ids each time and the id-normalised cards compare equal.

- [ ] 6.3 Record verification, review, score (≥95), and GitHub upload evidence in `docs/DELIVERY_WORKFLOW.md` for this slice, following the template used by the previous proposals. Include explicit pointers to the mapping table, the typed error codes, and the round-trip test. Verification: the delivery workflow entry exists with task rows 1.1 through 6.2 plus this recording task, and lists the `openspec archive` command + output.
