## 1. Contract foundations

- [ ] 1.1 Add `android/app/src/main/java/com/gkim/im/android/core/interop/SillyTavernCardCodec.kt` with helpers: `detectFormat(bytes)` (returns `Png`, `Json`, or `Unknown`), `hasPngTextChunk(bytes)` (rough check for any `tEXt` chunk presence), `estimateSize(bytes)`, and size constant declarations (`MaxPngBytes=8 MiB`, `MaxJsonBytes=1 MiB`). These are presentation / pre-upload sniffing only; decoding remains server-side. Verification: `$env:JAVA_HOME='C:\Program Files\Java\jdk-17'; .\gradlew.bat --no-daemon :app:compileDebugKotlin :app:testDebugUnitTest` stays green; new `SillyTavernCardCodecTest` covers PNG signature detection, JSON sniffing, unknown rejection, and size guard.

- [ ] 1.2 Extend `android/app/src/main/java/com/gkim/im/android/data/remote/im/ImBackendModels.kt` with DTOs for import/export: `CardImportUploadRequestDto` (multipart or base64 body + filename), `CardImportPreviewDto` (decoded deep persona record + warnings list + detected language + extensions under `st.*`), `CardImportCommitRequestDto`, `CardExportRequestDto` (format, language, optional `includeTranslationAlt`), `CardExportResponseDto` (binary wrapper for PNG / text wrapper for JSON). Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.ImBackendPayloadsTest` covers DTO round-trip including warning enum variants and the `st.*` extensions passthrough.

- [ ] 1.3 Extend `android/app/src/main/java/com/gkim/im/android/data/remote/im/ImBackendClient.kt` with `importCardPreview(baseUrl, token, bytes, filename)`, `importCardCommit(baseUrl, token, preview, overrides)`, `exportCard(baseUrl, token, cardId, format, language, includeTranslationAlt)`. Implement in `ImBackendHttpClient`. Provide default `error("not implemented")` stubs for backward-compat. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.ImBackendHttpClientTest` covers import success, oversize payload 413, malformed payload 422, export success for PNG + JSON, export language selection, and unknown cardId 404.

- [ ] 1.4 Finalize `openspec/changes/sillytavern-card-interop/specs/im-backend/spec.md` so import (preview + commit), export (PNG + JSON + language selection), validation rules, typed error codes, bounded safety limits, `st.*` extensions preservation, and dual-chunk PNG export are all captured as requirements with scenarios. Verification: `openspec validate sillytavern-card-interop --strict` passes.

## 2. Interop repository (Android)

- [ ] 2.1 Add `android/app/src/main/java/com/gkim/im/android/data/repository/CardInteropRepository.kt` interface exposing `previewImport(bytes, filename): Result<CardImportPreview>`, `commitImport(preview, overrides): Result<CompanionCharacterCard>`, `exportCard(cardId, format, language, includeTranslationAlt): Result<ExportedCardPayload>`, plus a `LiveCardInteropRepository` that binds to `ImBackendClient`. `DefaultCardInteropRepository` handles client-side size guards via `SillyTavernCardCodec` before upload. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.repository.CardInteropRepositoryTest` covers size-guard rejection, preview returning warnings list, commit surfacing the persisted card, export returning binary payload for PNG + text payload for JSON.

- [ ] 2.2 Register `cardInteropRepository` in `AppContainer` + `DefaultAppContainer` and expose it to tavern + card-detail view models. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.repository.*Test` stays green; `rg -n "cardInteropRepository" android/app/src/main/java` confirms wiring.

## 3. Android tavern: import entry point + preview

- [ ] 3.1 Add an "Import character card" entry point to the tavern surface overflow menu AND as a primary CTA on the empty-state of the roster. Tapping opens the system file picker filtered to `.png` and `.json`. On selection, the picked bytes are sniffed via `SillyTavernCardCodec`; oversized or unsupported files surface an inline error and do not navigate. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.TavernImportEntryPointTest` covers file-picker entry point render, size guard error rendering, sniff success navigating to preview.

- [ ] 3.2 Add `android/app/src/main/java/com/gkim/im/android/feature/tavern/ImportCardPreviewRoute.kt` backed by an `ImportCardPreviewViewModel` that uploads the bytes, renders the preview state (mapped deep persona record, detected source language with override, warnings list, `st.*` extensions summary). The user can edit any field inline, pick the language mapping, and tap Commit to persist. Cancel returns to the tavern without committing. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.ImportCardPreviewPresentationTest` covers preview-loaded state, warning rendering, language override, commit success routing back to roster.

- [ ] 3.3 Add instrumentation `CardImportInstrumentationTest` on `codex_api34` covering: open tavern → overflow → Import, pick a valid V2 PNG fixture, observe the preview with decoded persona + warnings + language picker, commit, observe the new card in the owned roster, then pick a malformed PNG fixture and observe the typed error. Verification: `.\gradlew.bat --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.gkim.im.android.feature.tavern.CardImportInstrumentationTest` passes.

## 4. Android card detail: export actions

- [ ] 4.1 Extend the tavern card-detail surface with "Export as PNG" and "Export as JSON" actions. Tapping either opens an export dialog with format confirmation, a language picker (`en` / `zh`, defaulted to the active `AppLanguage`), a toggle for "Include the other-language text as a `stTranslationAlt` extension" (default off), and a target toggle for `Share sheet` or `Save to Downloads`. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.CardDetailExportDialogTest` covers dialog state, default language selection, toggle defaults.

- [ ] 4.2 Wire the dialog to `CardInteropRepository.exportCard` and forward the returned payload to the Android system share sheet or `DownloadManager` depending on the user's choice. Surface errors inline if the backend rejects the export. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.CardExportInvocationTest` covers successful PNG share, successful JSON save, and failure routing.

- [ ] 4.3 Add instrumentation `CardExportInstrumentationTest` on `codex_api34` covering: open a card detail, invoke Export as PNG, confirm the share sheet is presented with a PNG payload; invoke Export as JSON, confirm the Downloads target receives a well-formed JSON file. Verification: `.\gradlew.bat --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.gkim.im.android.feature.tavern.CardExportInstrumentationTest` passes.

## 5. Backend contract alignment

- [ ] 5.1 Finalize `openspec/changes/sillytavern-card-interop/specs/im-backend/spec.md` to cover: `POST /api/cards/import` (preview) + `POST /api/cards/import/commit`, `GET /api/cards/{cardId}/export?format=...&language=...`, PNG tEXt chunk parsing, JSON schema validation for V2 and V3, bounded size + dimension limits with typed error codes, `st.*` extensions namespace preservation, dual-chunk PNG export (`chara` + `ccv3`), JSON export defaulting to V3 with opt-in V2-only mode, avatar re-encoding, heuristic language detection with user override. Verification: `openspec validate sillytavern-card-interop --strict` passes.

- [ ] 5.2 Finalize `openspec/changes/sillytavern-card-interop/specs/companion-character-card-depth/spec.md` delta so the `extensions` bag contract carries a reserved `st.*` namespace for ST round-trip fidelity (preserved on import, emitted on export), and so the card record's avatar-source semantics cover both user-captured images and imported PNG bytes. Verification: `openspec validate sillytavern-card-interop --strict` passes; the delta references the existing `Companion character cards carry a full persona authoring record` requirement without duplicating it.

- [ ] 5.3 Document in design.md § "Migration Plan" the exact ST-field → our-record mapping (the full table from the Context section), the typed error codes (`payload_too_large`, `avatar_too_large`, `unsupported_schema_version`, `malformed_png`, `malformed_json`), and the bounded safety limits. Verification: design.md references both the mapping table and the error-code list, and the spec deltas mention the error codes by name.

## 6. Verification and delivery evidence

- [ ] 6.1 Add focused unit suites: `SillyTavernCardCodecTest`, `CardInteropRepositoryTest`, `TavernImportEntryPointTest`, `ImportCardPreviewPresentationTest`, `CardDetailExportDialogTest`, `CardExportInvocationTest`, plus `ImBackendPayloadsTest` (interop DTOs) and `ImBackendHttpClientTest` (import/export endpoints). Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest` fully green.

- [ ] 6.2 Add instrumentation coverage on `codex_api34`: `CardImportInstrumentationTest` and `CardExportInstrumentationTest`, plus a round-trip suite `CardInteropRoundTripTest` that imports a V3 PNG fixture, commits, exports as V3 PNG, re-imports, and confirms the round-tripped deep persona record equals the first imported record (accounting for stable `id` reassignment). Verification: `.\gradlew.bat --no-daemon :app:connectedDebugAndroidTest` runs all three suites green.

- [ ] 6.3 Record verification, review, score (≥95), and GitHub upload evidence in `docs/DELIVERY_WORKFLOW.md` for this slice, following the template used by the previous proposals. Include explicit pointers to the mapping table, the typed error codes, and the round-trip test. Verification: the delivery workflow entry exists with task rows 1.1 through 6.2 plus this recording task, and lists the `openspec archive` command + output.
