## 1. Contract foundations

- [x] 1.1 Add `android/app/src/main/java/com/gkim/im/android/core/model/Lorebook.kt` carrying `id`, `ownerId`, `displayName: LocalizedText`, `description: LocalizedText`, `isGlobal`, `isBuiltIn`, `tokenBudget`, `extensions: JsonObject`, `createdAt`, `updatedAt`. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.core.model.LorebookModelTest` - pass (6 cases: data-class equality considers every field (`updatedAt`, `displayName`, `tokenBudget` perturbations all flip equality); `DefaultTokenBudget` constant is 1024 and a minimal lorebook defaults to tokenBudget 1024 / empty description / non-global / non-builtin / zeroed timestamps; `resolve()` returns active-language strings with `tokenBudget` / `isGlobal` / `isBuiltIn` propagated into `ResolvedLorebook`; `isDeletable` is true for user-owned and false for built-ins; `extensions` JsonObject survives `copy()` with `JsonPrimitive` values intact; `isGlobal` toggles independently of other fields). DTO-layer JSON round-trip is covered in task 1.4.

- [x] 1.2 Add `android/app/src/main/java/com/gkim/im/android/core/model/LorebookEntry.kt` carrying `id`, `lorebookId`, `name: LocalizedText`, `keysByLang: Map<AppLanguage, List<String>>`, `secondaryKeysByLang: Map<AppLanguage, List<String>>`, `secondaryGate: SecondaryGate`, `content: LocalizedText`, `enabled`, `constant`, `caseSensitive`, `scanDepth`, `insertionOrder`, `comment`, `extensions: JsonObject`. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.core.model.LorebookEntryTest` - pass (9 cases: data-class equality flips on `name` / `insertionOrder` / `secondaryGate` perturbations; defaults match spec (`SecondaryGate.None`, `LocalizedText.Empty` content, `enabled=true`, `constant=false`, `caseSensitive=false`, `scanDepth=3`, `DefaultScanDepth=3`, `MaxServerScanDepth=20`, `insertionOrder=0`, empty key maps); `primaryKeysFor(language)` returns per-language list or empty when language absent; `secondaryKeysFor(language)` returns per-language list; `canMatchInLanguage` is true when `constant=true` even with empty keys, false when no keys and not constant, false when all keys blank; `extensions` JsonObject survives `copy()` with `JsonPrimitive` intact; `SecondaryGate` covers `None` / `And` / `Or`). DTO-layer JSON round-trip is covered in task 1.4.

- [x] 1.3 Add `android/app/src/main/java/com/gkim/im/android/core/model/LorebookBinding.kt` tying a `lorebookId` to a `characterId` with a `isPrimary: Boolean` flag used at export time. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.core.model.LorebookBindingTest` - pass (6 cases: data-class equality flips on `characterId` / `isPrimary` perturbations; default `isPrimary` is `false`; `primaryFor(characterId)` returns the binding flagged primary when one exists, null when no primary exists for that character, null when the character has no bindings; `lorebookIdsBoundTo(characterId)` collects every binding regardless of primary flag and returns empty for unbound characters). DTO-layer JSON serialization is covered in task 1.4.

- [x] 1.4 Extend `android/app/src/main/java/com/gkim/im/android/data/remote/im/ImBackendModels.kt` with DTOs for lorebook CRUD and binding CRUD, plus the bootstrap response extension that carries the user's lorebook summary for the Settings entry's "X lorebooks" badge. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.ImBackendPayloadsTest` - pass. New DTOs: `LorebookDto` + `LorebookListDto` + `CreateLorebookRequestDto` + `UpdateLorebookRequestDto` + `LorebookSummaryDto` (bootstrap badge); `LorebookEntryDto` + `LorebookEntryListDto` + `CreateLorebookEntryRequestDto` + `UpdateLorebookEntryRequestDto` (with nullable-opt-in partial updates); `LorebookBindingDto` + `LorebookBindingListDto` + `CreateLorebookBindingRequestDto` + `UpdateLorebookBindingRequestDto`; `PerLanguageStringListDto` wrapper with `toLanguageMap()` / `fromLanguageMap()` for per-language keys. `BootstrapBundleDto` extended with `lorebookSummaries: List<LorebookSummaryDto>` defaulting to empty. `secondaryGate` serialized as uppercase string ("NONE"/"AND"/"OR") with case-insensitive decoding. Test coverage (22 new cases): bilingual lorebook passthrough + defaults + fromLorebook round-trip + list wrapper + summary shape + bootstrap bundle carries summaries + bootstrap defaults when absent + create/update request round-trips + per-language map conversion with empty-list language omission + secondary gate variant coverage (None/And/Or) + lowercase-gate decoding tolerance + per-language key preservation through JSON + entry defaults match domain + entry fromLorebookEntry round-trip + entry list + create/update entry request round-trip + binding primary flag + binding default primary false + binding fromLorebookBinding round-trip + binding list + create/update binding request round-trip.

## 2. Lorebook repository and API wiring

- [x] 2.1 Add `android/app/src/main/java/com/gkim/im/android/data/remote/im/ImWorldInfoClient.kt` with `list()`, `create(request)`, `update(id, request)`, `delete(id)`, `duplicate(id)`, `listEntries(lorebookId)`, entry CRUD, `listBindings(lorebookId)`, `bind(lorebookId, characterId, isPrimary)`, `unbind(lorebookId, characterId)`. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.ImWorldInfoClientTest` - pass (19 cases: GET /api/lorebooks + 401 error; GET /api/lorebooks/{id}; POST /api/lorebooks with 400 error path; PATCH /api/lorebooks/{id} with partial body; DELETE /api/lorebooks/{id} + 409 `lorebook_has_bindings`; POST /api/lorebooks/{id}/duplicate; GET /api/lorebooks/{id}/entries; POST /api/lorebooks/{id}/entries; PATCH /api/lorebooks/{id}/entries/{entryId} with partial body; DELETE /api/lorebooks/{id}/entries/{entryId}; GET /api/lorebooks/{id}/bindings; POST bindings + 409 `binding_exists`; PATCH /api/lorebooks/{id}/bindings/{characterId}; DELETE /api/lorebooks/{id}/bindings/{characterId} + 404 not-found). Retrofit `@HTTP(method = "PATCH", hasBody = true)` is used so the DTO's nullable-opt-in partial update shape is preserved on the wire.

- [x] 2.2 Add `android/app/src/main/java/com/gkim/im/android/data/repository/WorldInfoRepository.kt` with `StateFlow<List<Lorebook>>`, `StateFlow<Map<LorebookId, List<LorebookEntry>>>`, and CRUD methods that optimistically apply the mutation and reconcile with the server response. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.data.repository.WorldInfoRepositoryTest` - pass (24 cases). Delivered: `WorldInfoRepository` interface exposing `observeLorebooks / observeEntries / observeBindings` StateFlows + lorebook/entry/binding CRUD returning a `WorldInfoMutationResult<T>` sealed result (Success / Rejected with `RejectionReason.{UnknownLorebook, UnknownEntry, UnknownBinding, BuiltInLorebookImmutable, LorebookHasBindings, BindingAlreadyExists}` / Failed). `DefaultWorldInfoRepository` holds state in three MutableStateFlows, generates ids + timestamps through injectable fns, demotes prior primary bindings across all other lorebooks via `applyPrimarySweep`, duplicates lorebooks by copying entries with fresh ids + bilingual "(copy) / （副本）" suffix, drops entries + bindings on lorebook deletion, rejects deleting built-ins or lorebooks with bindings, and exposes snapshot helpers (`setSnapshot` / `replaceLorebook` / `replaceEntries` / `replaceBindings` / `removeLorebook`) for the Live decorator. `LiveWorldInfoRepository` wraps the default repo and an `ImWorldInfoClient`, applying every mutation locally first, then calling the remote and reconciling with the server DTO on success or rolling back to the pre-mutation snapshot on failure; `refresh()` loads lorebooks + per-lorebook entries + bindings sequentially and skips when baseUrl/token are absent. Test coverage (24 cases, Default layer = 12 cases, Live layer = 12 cases): (Default) createLorebook assigns id+clock and exposes via StateFlow; updateLorebook preserves createdAt + isBuiltIn; updateLorebook rejects UnknownLorebook; updateLorebook rejects BuiltInLorebookImmutable; deleteLorebook rejects LorebookHasBindings; deleteLorebook drops entries + bindings when none remain; duplicateLorebook copies entries with fresh ids; createEntry rewrites lorebookId + assigns id; createEntry rejects UnknownLorebook; updateEntry rejects UnknownEntry; bind rejects BindingAlreadyExists; bind sweeps prior primary on other lorebooks for the same character; updateBinding toggles isPrimary and propagates primary sweep; unbind rejects UnknownBinding. (Live) createLorebook reconciles server-assigned id; createLorebook rolls back when server rejects; updateLorebook rolls back on server failure; deleteLorebook rolls back preserving entries + bindings; createEntry reconciles server-assigned id; updateEntry rolls back to prior entry on server failure; bind propagates primary sweep and records server reconciliation; updateBinding rolls back primary sweep on failure; refresh loads lorebooks + entries + bindings from server; refresh no-ops when baseUrl or token is missing.

- [x] 2.3 Wire `WorldInfoRepository` into the same DI graph as `PresetRepository` / `MemoryRepository`. Refresh on bootstrap and on authenticated login. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.data.repository.RepositoryBootstrapTest` - pass (4 cases). Delivered: `AppContainer` grew `val worldInfoRepository: WorldInfoRepository`; `DefaultAppContainer` constructs `LiveWorldInfoRepository(DefaultWorldInfoRepository(), ImWorldInfoHttpClient(okHttpClient), baseUrlProvider = { sessionStore.baseUrl }, tokenProvider = { sessionStore.token })` and wires `onBootstrapLoaded = { worldInfoRepository.refresh() }` into `LiveMessagingRepository`. `LiveMessagingRepository` now accepts an optional `onBootstrapLoaded: (suspend () -> Unit)?` param and calls a private `runPostBootstrapHook()` (which swallows hook exceptions via `runCatching`) after every `applyBootstrapState` site: dev-session bootstrap, authenticated-session bootstrap, and the `resyncAfterReconnect` path. The three `androidTest` fake `AppContainer`s (`GkimRootAppTest.UiTestAppContainer`, `LiveImageSendValidationTest`, `LoginEndpointConfigurationTest`) pick up `override val worldInfoRepository: WorldInfoRepository = DefaultWorldInfoRepository()`. Test coverage (4 cases): dev-session bootstrap fires the hook strictly after `loadBootstrap` (tick-clock ordering proves `hook.invokedAt > bootstrap.at`); authenticated-session bootstrap fires the hook strictly after `loadBootstrap` (stored-token path); bootstrap still reaches `Ready` when the hook itself throws (hook errors are isolated from the session pipeline); bootstrap still reaches `Ready` when no hook is registered (opt-in parameter remains backwards-compatible).

## 3. Settings → Companion → World Info library UI

- [x] 3.1 Add Settings entry under `Companion` (already defined in `companion-settings-and-safety-reframe`) labeled "World Info" that routes to `feature/settings/worldinfo/WorldInfoLibraryRoute.kt`. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.feature.settings.SettingsMenuPresentationTest` - pass (6 cases). Delivered: `SettingsDestination` is now `internal` and gained a `WorldInfo` value; menu composition was extracted into a pure `internal fun buildSettingsMenuItems(SettingsUiState): List<SettingsMenuItem>` that the Compose `SettingsMenuScreen` iterates; `SettingsScreen` dispatch gained a `SettingsDestination.WorldInfo -> WorldInfoLibraryRoute(...)` branch; new stub `android/app/src/main/java/com/gkim/im/android/feature/settings/worldinfo/WorldInfoLibraryRoute.kt` renders a bilingual `PageHeader("World Info" / "世界信息")` + placeholder text behind testTag `worldinfo-library-screen` (full content lands in task 3.2). Test coverage (6 cases): world-info entry exists with correct testTag + destination + bilingual labels + bilingual summaries; menu preserves appearance/ai-provider/im-validation/personas/worldinfo/account entries; world info sits between personas and account for Companion grouping; `SettingsDestination.WorldInfo` enum value is usable from tests; AI-provider summary reflects active provider + falls back to "Choose a provider" / "选择提供商" when absent; connection summary surfaces `imValidationError` when present and otherwise shows bilingual "Backend" / "后端" prefix with trailing slash removed.

- [x] 3.2 Add `feature/settings/worldinfo/WorldInfoLibraryRoute.kt` that renders the user's lorebook list with per-row (name, entry count, `Global` badge), the primary Create CTA, per-row overflow (Duplicate, Delete disabled when bound, Toggle global), and row-tap routing into the editor. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.feature.settings.worldinfo.WorldInfoLibraryPresentationTest` - pass (13 cases). Delivered: `WorldInfoLibraryViewModel` combines `observeLorebooks / observeEntries / observeBindings` + an internal `lorebookSnapshot` MutableStateFlow with pending-operation + error-message state, exposing `WorldInfoLibraryUiState(rows = List<WorldInfoLibraryRow>, pendingOperation, errorMessage)`. `WorldInfoLibraryRow` carries `lorebookId / displayName / entryCount / isGlobal / isBuiltIn / boundCharacterCount` with derived `hasBindings / canDelete / canDuplicate / canToggleGlobal` flags — `canDelete = !isBuiltIn && !hasBindings`, `canToggleGlobal = !isBuiltIn`. ViewModel actions: `createLorebook(ownerId)` seeds a bilingual "New lorebook" / "新世界书" draft; `duplicate`, `delete`, `toggleGlobal` delegate to the repo and surface bilingual rejection messages via `handleMutationOutcome`; `clearError` resets the banner. Compose `WorldInfoLibraryRoute(container, onBack, onOpenLorebook)` renders `PageHeader` + Create button (`worldinfo-library-create` testTag) + empty-state card + error card + per-row `Surface` (onClick → `onOpenLorebook(lorebookId)`) with inline name + bilingual entry-count text + optional Global badge + optional "bound to N character(s)" caption + overflow `DropdownMenu` carrying Duplicate/Set-global/Delete items (each disabled when its `can*` flag is false). Sorting promotes built-ins above user-owned lorebooks, ordered within each group by `createdAt`. Test coverage (13 cases): rows expose resolved displayName+entryCount per lorebook; displayName falls back to "Untitled lorebook" / "未命名世界书" when both languages are blank; Global badge flag reflects `isGlobal`; Delete disabled when lorebook has bindings; Delete disabled for built-in lorebooks even when unbound; Create CTA seeds bilingual `New lorebook` / `新世界书` default and assigns ownerId; Duplicate produces a sibling lorebook with a `(copy)` / `（副本）` suffix; Delete removes an unbound lorebook; Delete surfaces bilingual `Lorebook still bound to characters` error when bound; toggleGlobal flips `isGlobal` back and forth; toggleGlobal is a no-op for built-in lorebooks; clearError resets the error banner; built-ins sort before user-owned lorebooks within each group by createdAt.

- [x] 3.3 Add `feature/settings/worldinfo/WorldInfoEditorRoute.kt` that renders the lorebook header editor (displayName, description, tokenBudget, isGlobal), the entry list with move-up/down + enabled toggle + overflow, and the bindings sub-surface (list bound characters + bind picker + unbind). Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.feature.settings.worldinfo.WorldInfoEditorPresentationTest` - pass (16 cases). Delivered: `WorldInfoEditorViewModel` subscribes to `observeLorebooks / observeEntries / observeBindings` + `rosterRepository.presetCharacters / userCharacters` and projects `WorldInfoEditorUiState(header, entries, bindings, bindablePickerItems, pendingOperation, errorMessage)`. Header carries `englishName / chineseName / englishDescription / chineseDescription / tokenBudget / isGlobal / isBuiltIn`; entry rows expose `displayName / enabled / insertionOrder / position / total` with derived `canMoveUp` (position > 0) and `canMoveDown` (position < total-1). Entries are sorted by `insertionOrder` ascending (tie-broken by id). Actions: `saveHeader(...)` calls `updateLorebook` with new LocalizedText + tokenBudget + isGlobal; `addEntry(...)` seeds bilingual "New entry" / "新条目" at `insertionOrder = max + 1`; `moveEntryUp/Down` swaps `insertionOrder` with the neighbor and safely no-ops at the boundary; `toggleEntryEnabled` flips `enabled`; `deleteEntry` calls `deleteEntry(lorebookId, entryId)`; `bindCharacter` defaults primary to false; `unbindCharacter` removes the binding; `togglePrimaryBinding` flips `isPrimary` via `updateBinding`. The picker excludes already-bound characters and resolves display names via `resolve(language)`. Compose `WorldInfoEditorRoute` renders three `GlassCard` sections (header, entries, bindings) plus an error banner; header controls disable themselves when `isBuiltIn`; entry row exposes enabled `Switch` + `↑` / `↓` buttons (disabled at the ends) + overflow menu with `Delete`; bindings card shows a `Bind character` dropdown populated from `bindablePickerItems` and per-row `Unbind` / `Set primary` / `Unset primary` buttons. `SettingsRoute` gained a `WorldInfoEditor` destination + `editingLorebookId` state so the library's row tap routes here. Test coverage (16 cases): header exposes resolved fields; saveHeader dispatches updateLorebook; saveHeader surfaces "Built-in lorebook cannot be modified" when the lorebook is built-in; entries sorted by insertionOrder with canMoveUp/canMoveDown gated at boundaries; addEntry appends above the current max insertionOrder with bilingual default names; moveEntryUp/Down swap adjacent insertionOrder values; moveEntryUp at the top is a safe no-op; toggleEntryEnabled flips enabled; deleteEntry removes the entry; bindings resolve display names through the companion roster; bind CTA leaves primary flag false by default; picker excludes already-bound characters; unbindCharacter removes the binding; togglePrimaryBinding flips the primary flag; header is null when lorebookId doesn't exist.

- [x] 3.4 Add `feature/settings/worldinfo/WorldInfoEntryEditor.kt` (modal or deep route) exposing the full entry field set: `name` bilingual, two per-language key tabs (en/zh) with inline add/remove, secondary keys + gate dropdown, bilingual content, constant/caseSensitive toggles, scanDepth/insertionOrder numbers, comment textarea. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.feature.settings.worldinfo.WorldInfoEntryEditorPresentationTest` - pass (17 cases). Delivered: `WorldInfoEntryEditorViewModel` exposes `WorldInfoEntryEditorUiState(entryId, lorebookId, loaded, englishName, chineseName, englishKeys, chineseKeys, englishSecondaryKeys, chineseSecondaryKeys, secondaryGate, englishContent, chineseContent, enabled, constant, caseSensitive, scanDepth, insertionOrder, comment, errorMessage, saveCompleted)` combined from three internal `MutableStateFlow`s (`draftState`, `errorMessageState`, `saveCompletionState`). `init` subscribes to `repository.observeEntries()` and seeds the draft on the first matching emit using a `loaded` guard so subsequent upstream reconciliations don't clobber in-progress user edits. Setters: `setEnglishName/setChineseName`, `setEnglishKeys/setChineseKeys`, `addKey(language, value)` (trims + ignores blank), `removeKey(language, index)` (bounds-checked), `addSecondaryKey/removeSecondaryKey`, `setSecondaryGate`, `setEnglishContent/setChineseContent`, `setEnabled/setConstant/setCaseSensitive`, `setScanDepth(value)` (clamped to `0..LorebookEntry.MaxServerScanDepth`), `setInsertionOrder`, `setComment`, `clearError`. `save()` calls `repository.updateEntry(draft.toEntry())`, increments `saveCompletionState` on Success, surfaces bilingual English error messages on Rejected (`UnknownEntry → "Entry not found"`, `UnknownLorebook → "Lorebook not found"`, fallback → "Operation rejected"); short-circuits with "Entry not loaded yet" if `loaded == false`. `toEntry()` filters empty-list languages out of the `keysByLang` / `secondaryKeysByLang` maps. Compose `WorldInfoEntryEditorRoute(container, lorebookId, entryId, onBack)` renders a vertically scrolling `PageHeader` + seven `GlassCard` sections: name (testTags `worldinfo-entry-editor-name-english/chinese`), primary keys (`worldinfo-entry-editor-keys-card` with a reusable `LanguageKeyTabs` composable carrying a two-tab `TabRow` that isolates `englishKeys` / `chineseKeys` lists + per-row remove + a shared add input), secondary keys (`worldinfo-entry-editor-secondary-card` reusing `LanguageKeyTabs` plus a `DropdownMenu` gate picker with items for None/AND/OR), bilingual content, flags (enabled/constant/caseSensitive `Switch`es), digit-filtered scan-depth (label shows `0–${LorebookEntry.MaxServerScanDepth}`) + insertion-order fields, and a comment textarea. Save button dispatches `viewModel.save()`; a `LaunchedEffect(uiState.saveCompleted)` calls `onBack()` once the completion counter ticks upward. `SettingsRoute` grew a new `SettingsDestination.WorldInfoEntryEditor` + `editingEntryId` state + `onOpenEntry` / `onEntryEditorDone` callbacks; the existing `WorldInfoEditorRoute`'s `onOpenEntry` parameter (added in 3.3) now routes entry-row taps into the new screen. Test coverage (17 cases): draft seeds from the repository across every field (name/keys/secondary/gate/content/flags/scanDepth/insertionOrder/comment); `setEnglishName` / `setChineseName` update independently; `addKey` trims and targets only the requested language, blank input is a no-op; `removeKey` targets only the requested language and is a safe no-op on an out-of-range index; `addSecondaryKey` / `removeSecondaryKey` operate on the secondary list without touching primary keys; `setSecondaryGate` covers None/And/Or; `setEnglishContent` / `setChineseContent` update the draft; `setEnabled` / `setConstant` / `setCaseSensitive` flip independently; `setScanDepth` clamps to `0..MaxServerScanDepth`; `setInsertionOrder` / `setComment` round-trip; `save` writes every field back to `repo.entries.value` and increments `saveCompleted`; `save` strips empty-language lists so only populated languages persist; `save` surfaces "Entry not loaded yet" when the entry is missing; `clearError` resets the banner without discarding unsaved draft edits; upstream `updateEntry` emissions after the initial load do not clobber in-progress draft edits.

## 4. Character card-detail lorebook tab

- [x] 4.1 Extend `feature/tavern/CharacterDetailRoute.kt` (or the equivalent card-detail surface) with a **Lorebook** tab that renders a read-only preview of every lorebook currently bound to this character. Each lorebook shows its displayName and entry count; tapping "Manage in library" routes to the editor. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.CharacterDetailLorebookTabTest` - pass (10 cases). Delivered: new `android/app/src/main/java/com/gkim/im/android/feature/tavern/CharacterLorebookTab.kt` exposing `CharacterLorebookRow(lorebookId, displayName, entryCount, isPrimary)` + `CharacterLorebookTabUiState(characterId, rows)` with `isEmpty` derived flag; `CharacterLorebookTabViewModel(repository, characterId, language)` combines `observeLorebooks / observeEntries / observeBindings`, filters `bindings.values.flatten()` by `characterId`, resolves `displayName` via `lorebook.displayName.resolve(language())` with bilingual "Untitled lorebook" / "未命名世界书" fallback when blank, entry count from `entries[lorebook.id].orEmpty().size`, isPrimary carried from the binding, and sorts `compareByDescending { isPrimary }.thenBy { displayName }`; rows that reference missing lorebooks are filtered out. Compose `CharacterLorebookTabSection(container, characterId, onManageLorebook)` renders `GlassCard(testTag "character-detail-lorebook-tab")` containing a bilingual "Bound lorebooks" / "已绑定世界书" title + empty-state Text (`character-detail-lorebook-empty`, bilingual "No lorebooks bound yet." / "尚未绑定任何世界书。") OR per-row `CharacterLorebookRowView` (testTag `character-detail-lorebook-row-{id}`) with `-name`, `-entry-count` (bilingual "N entries" / "N 条"), conditional `-primary-badge` (bilingual "Primary" / "主要"), and a `-manage` TextButton ("Manage in library" / "前往管理") invoking `onManageLorebook(row.lorebookId)`. `CharacterDetailRoute` grew a `lorebookTab: @Composable () -> Unit = {}` slot in `CharacterDetailScreen`, wired right after the metadata card, populated by `CharacterLorebookTabSection(container, card.id, onManageLorebook = { id -> navController.navigate("settings?worldinfoLorebookId=$id") })`. Navigation: `GkimRootApp.kt`'s `composable("settings")` is now `composable("settings?worldinfoLorebookId={worldinfoLorebookId}")` extracting the nullable-blank-filtered argument into `SettingsRoute(initialWorldInfoLorebookId = ...)`. `SettingsRoute` gained an `initialWorldInfoLorebookId: String? = null` parameter that, when non-null, seeds `destination = SettingsDestination.WorldInfoEditor` + `editingLorebookId = initialWorldInfoLorebookId` through `rememberSaveable(initialWorldInfoLorebookId)` so the seed re-applies on navigation back-and-forth. Test coverage (10 cases): empty-state exposes no rows when character has no bindings; rows expose displayName + entry count per bound lorebook; rows resolve displayName in the active language (Chinese variant verified); rows fall back to "Untitled lorebook" / "未命名世界书" when both language strings blank (both English and Chinese view models verified); rows only include bindings for the target character (excludes `characterId = "someone-else"` binding); primary binding sorts before non-primary and `isPrimary` flag is exposed; rows sort alphabetically inside each primary bucket (Alpha/Beta/Gamma ordering verified); missing lorebook referenced by a binding is filtered out (ghost binding dropped); manage callback fires with the tapped lorebookId for deep-link into the editor; rows update live when a new binding is added via `repo.bind(...)`.

- [x] 4.2 When a character has zero bound lorebooks, the tab renders a zero-state with a "Bind a lorebook" CTA that opens a picker of the user's lorebooks. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.CharacterDetailLorebookTabTest` - pass (19 cases total, 9 new on top of the 10 from 4.1). Delivered: extended `CharacterLorebookTabUiState` with `pickerItems: List<CharacterLorebookPickerItem>` + `errorMessage: String?` + derived `canBind` flag. `CharacterLorebookTabViewModel` now combines a 4th flow (`errorMessageState: MutableStateFlow<String?>`) and derives `pickerItems` as all lorebooks whose ids are not in the set of lorebookIds currently bound to this character, sorted alphabetically by resolved displayName (with bilingual "Untitled lorebook" / "未命名世界书" fallback when blank). `bind(lorebookId)` dispatches `repository.bind(lorebookId, characterId, isPrimary = false)` via `viewModelScope.launch` and surfaces bilingual error messages on Rejected (`UnknownLorebook → "Lorebook not found"` / "未找到世界书", `BindingAlreadyExists → "Lorebook already bound"` / "世界书已绑定", fallback → "Operation rejected" / "操作被拒绝") and Failed ("Bind failed" / "绑定失败"). `clearError()` resets the banner. Compose: `CharacterLorebookTabSection` renders an optional error banner (testTag `character-detail-lorebook-error`) with a Dismiss TextButton (`character-detail-lorebook-error-dismiss`), followed by the existing empty-text OR rows, followed by a `BindLorebookControl`. The control is a `TextButton` ("Bind a lorebook" / "绑定世界书") gated by `pickerItems.isNotEmpty()` that opens a `DropdownMenu` (`character-detail-lorebook-picker`) populated with `DropdownMenuItem(testTag "character-detail-lorebook-picker-{id}")` per picker entry invoking `viewModel::bind` on click; when `pickerItems` is empty the menu shows a disabled "No lorebooks available" / "暂无可绑定世界书" placeholder. The button's testTag shifts between `character-detail-lorebook-bind-cta` (zero-state) and `character-detail-lorebook-bind-another` (non-zero state) so tests can distinguish the two surfaces. New test coverage (9 cases): `pickerItems expose unbound lorebooks sorted alphabetically` (also verifies `canBind = true`); `pickerItems exclude lorebooks already bound to this character`; `pickerItems still include lorebooks bound only to other characters` (character-scoped filter); `canBind is false when there are no pickerItems`; `pickerItems fall back to Untitled when displayName is blank` (English + Chinese variants); `bind creates a non-primary binding for this character` (verifies row materialization + picker item removed + errorMessage null); `bind surfaces bilingual error when lorebook is already bound` (English + Chinese variants); `bind surfaces bilingual error when lorebook is unknown`; `clearError resets the error banner`.

## 5. Backend contract alignment

- [x] 5.1 Finalize `openspec/changes/world-info-binding/specs/im-backend/spec.md` to cover: lorebook CRUD endpoints, entry CRUD endpoints, binding CRUD endpoints, scan algorithm behavior, the allocator integration (placing `worldInfoEntries` between `userPersonaDescription` and `rollingSummary`), and the import/export round-trip with ST `character_book`. Verification: `openspec validate world-info-binding --strict` - pass (`Change 'world-info-binding' is valid`). Delivered: `im-backend/spec.md` carries four ADDED Requirements — (1) Authenticated CRUD for lorebooks, entries, and bindings (enumerating the `/api/lorebooks`, `/api/lorebooks/{id}/entries/*`, and `/api/lorebooks/{id}/bindings/*` endpoints with scenarios for user-scoped listing, entry CRUD being scoped to the parent lorebook with lorebookId rewrite + `not_found` rejection, delete-while-bound rejection via `lorebook_has_bindings`, binding creation with `binding_exists` rejection, and primary-sweep on `isPrimary=true` updates); (2) Deterministic single-pass keyword scan (candidate set = union of character-bound + user-global lorebooks deduplicated, scan text = current turn + up to scanDepth prior turns capped at 20, literal substring matching with per-entry case sensitivity, `constant=true` always included, total order `(insertionOrder asc, lorebookId asc, entryId asc)` with scenarios for candidate dedup, cap enforcement, and cross-run determinism); (3) Allocator integration placing `worldInfoEntries` between `userPersonaDescription` (above) and `rollingSummary` (below), with per-lorebook `tokenBudget` dropped before section budget and scenarios covering section priority, per-lorebook drop, and reverse-total-order section drop; (4) Import/export round-trip materializing a `Lorebook` + `isPrimary=true` `LorebookBinding` from imported `character_book` on commit and emitting the primary-bound lorebook into exported `character_book` with `extensions.st.*` preservation + non-primary warning scenario.

- [x] 5.2 Cross-reference the allocator integration from design.md §4 in the `world-info-binding` new-capability spec (scan ordering) and in the `im-backend` delta (allocator clause), ensuring both name `userPersonaDescription` (above) and `rollingSummary` (below) as the neighboring sections in priority. Verification: `rg -n "userPersonaDescription" openspec/changes/world-info-binding/specs/` - pass (hits in both `world-info-binding/spec.md` and `im-backend/spec.md`). Delivered: strengthened the `world-info-binding` new-capability "Matched entries are sorted by a deterministic total order" requirement to state explicitly "The `worldInfoEntries` section MUST sit between the `userPersonaDescription` section (above) and the `rollingSummary` section (below) in the allocator's priority ordering, matching the allocator clause in the `im-backend` delta so client-side expectations stay aligned with server behavior." and added a "Section priority sits between `userPersonaDescription` and `rollingSummary`" scenario echoing the server-side section-priority scenario in the im-backend delta.

- [x] 5.3 Cross-reference the ST `character_book` round-trip contract in the new-capability spec and the `im-backend` delta so the canonical lossless slot name is unambiguous. Verification: `rg -n "character_book" openspec/changes/world-info-binding/specs/` - pass (hits in three files: `world-info-binding/spec.md`, `im-backend/spec.md`, and `core/im-app/spec.md` — the capability requirement plus the server and client-side import-preview requirements all name `character_book` explicitly as the canonical slot).

## 6. Import / export integration

- [x] 6.1 Extend the character-card import preview (from `sillytavern-card-interop`) so an imported V2/V3 card carrying `character_book` renders a lorebook-import summary (entry count + token estimate + flag whether `constant` entries exist) alongside the existing preview fields. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.CardImportLorebookPreviewTest` - pass (11 cases). Delivered: new domain `LorebookImportSummary(entryCount, totalTokenEstimate, hasConstantEntries)` in `data/repository/CardInteropRepository.kt` + `CardImportPreview.lorebookSummary: LorebookImportSummary? = null`; wire DTO `LorebookImportSummaryDto(entryCount, totalTokenEstimate = 0, hasConstantEntries = false)` in `data/remote/im/ImBackendModels.kt` plus `CardImportPreviewDto.lorebookSummary: LorebookImportSummaryDto? = null` with defaults-friendly decoding for existing server payloads. `LiveCardInteropRepository.toDomain()` threads the summary into the domain; `LiveCardInteropRepository.commitImport()` passes the summary back to the server inside the `CardImportPreviewDto` sent with the commit request. `ImportCardPreviewRoute` renders a new `GlassCard(testTag "tavern-import-preview-lorebook-summary")` after the ST-extensions row whenever `state.preview.lorebookSummary != null`, containing a title ("Lorebook import" / "世界书导入"), a bilingual entry-count row (`tavern-import-preview-lorebook-entry-count`), a bilingual token-estimate row (`tavern-import-preview-lorebook-token-estimate`, "Estimated tokens: N" / "预估 token：N"), and when `hasConstantEntries` is true a bilingual constant-flag row (`tavern-import-preview-lorebook-constant-flag`, "Contains always-on entries (constant = true)" / "包含常驻条目（constant = true）"). Entry-count formatting is factored into the internal pure helper `lorebookSummaryEntryCountCopy(summary, english)` so the pluralisation rule is test-friendly: English uses singular "1 entry" and plural "N entries"; Chinese uses "N 条条目". Test coverage (11 cases): `lorebookSummary is null by default`; `lorebookSummary carries entry count token estimate and constant flag`; `loaded state exposes lorebookSummary through the ViewModel`; `entryCountCopy uses singular entry in English when count is 1`; `entryCountCopy pluralises entries in English when count is greater than 1`; `entryCountCopy renders bilingual zh string`; `entryCountCopy handles zero entries`; `LorebookImportSummaryDto round-trips over json with defaults for optional fields`; `CardImportPreviewDto decodes a payload that omits lorebookSummary as null` (backwards-compatible with old server); `CardImportPreviewDto decodes lorebookSummary when present`; `non-lorebook preview omits summary at the ViewModel layer`.

- [ ] 6.2 Extend the character-card import commit path so a new `Lorebook` is created per imported `character_book`, its entries seeded from `character_book.entries`, the new lorebook bound to the imported character (with `isPrimary = true`), and un-modeled fields preserved under `entry.extensions.st.*`. Verification: instrumentation test `CardImportLorebookMaterializationInstrumentationTest` on `codex_api34` imports a fixture ST card with a `character_book`, walks through preview → commit, then asserts the lorebook exists in the library and the binding is present on the character detail surface.

- [ ] 6.3 Extend the character-card export endpoint so the character's primary-bound lorebook is emitted into `character_book` losslessly (modeled fields + `extensions.st.*`), monolingual key/content selected by the exporting user's primary-language choice with the other-language payload preserved under `entry.extensions.stTranslationAlt.*`. When multiple lorebooks are bound, non-primary bindings surface as a warning in the export response. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.CardExportLorebookRoundTripTest` covers: (a) export → re-import round-trip preserves entry set and `extensions.st.*`, (b) multiple-binding warning surfaces.

## 7. Runtime injection smoke check

- [ ] 7.1 Add a developer-only debug endpoint (gated on `BuildConfig.DEBUG` + dev-header) that accepts a candidate character id + a simulated scan text and returns the entries that would match, sorted by `insertionOrder`. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.WorldInfoDebugScanTest` covers the request/response shape.

- [ ] 7.2 Add an instrumentation smoke test on `codex_api34`: seed a lorebook with three entries (one `constant`, two keyword-gated with distinct keys), bind it to the active character, submit a turn whose body matches one of the keyword keys, and assert via the debug endpoint that exactly the `constant` entry plus the matched entry fire. Verification: `.\gradlew.bat --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.gkim.im.android.feature.tavern.WorldInfoRuntimeSmokeInstrumentationTest` passes.

## 8. Verification and delivery evidence

- [ ] 8.1 Add focused unit suites: `LorebookModelTest`, `LorebookEntryTest`, `LorebookBindingTest`, `ImWorldInfoClientTest`, `WorldInfoRepositoryTest`, `WorldInfoLibraryPresentationTest`, `WorldInfoEditorPresentationTest`, `WorldInfoEntryEditorPresentationTest`, `CharacterDetailLorebookTabTest`, `CardImportLorebookPreviewTest`, `CardExportLorebookRoundTripTest`, `WorldInfoDebugScanTest`. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest` fully green.

- [ ] 8.2 Add instrumentation coverage on `codex_api34`: `CardImportLorebookMaterializationInstrumentationTest` + `WorldInfoRuntimeSmokeInstrumentationTest`. Verification: `.\gradlew.bat --no-daemon :app:connectedDebugAndroidTest` runs both suites green.

- [ ] 8.3 Record verification, review, score (≥95), and GitHub upload evidence in `docs/DELIVERY_WORKFLOW.md` for this slice following the template used by prior proposals. Include explicit pointers to the scan-algorithm table, the allocator slot, and the round-trip mapping with `character_book`. Verification: the delivery workflow entry exists with task rows 1.1 through 8.2 plus this recording task, and lists the `openspec archive` command + output.
