## 1. Space Chrome

- [ ] 1.1 Restore `AI 工具` and `动态` in the `Space` filter row, remove the top-level unread summary card, and update the affected `Space` UI coverage.
- [ ] 1.2 Remove the `创作者动态` eyebrow and the current developer-explanatory description text from the `Space` header, then add regression checks that production-facing `Space` chrome no longer renders those helper strings.
