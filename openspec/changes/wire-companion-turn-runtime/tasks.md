## 1. Contract reconnaissance

- [x] 1.1 Read `data/repository/CompanionTurnRepository.kt` + `data/repository/LiveCompanionTurnRepository.kt` and confirm whether `submitUserTurn`, an observable state flow per conversation, and a retry API are already present on the repository surface. Record the findings (API shapes, missing pieces if any) in `openspec/changes/wire-companion-turn-runtime/design.md`. If a missing method blocks §3, list the minimal additive contract delta needed and flag it explicitly so §3 can cover it; do not change the repository surface in this task. Verification: `design.md` exists in this change dir with the recon note + any flagged contract delta; `openspec validate wire-companion-turn-runtime --strict` passes on the skeleton.

## 2. Conversation companion marker

- [x] 2.1 Add a companion-conversation marker to `core/model/Conversation.kt` (prefer `companionCardId: String?` so it is non-breaking for peer-IM call sites; fall back to a `kind: ConversationKind` enum only if the string-ID path collides with existing fields). Thread the marker through `data/repository/MessagingRepository.ensureConversation(...)` so `CharacterDetailRoute.onActivate` can construct a companion conversation. Verification: `$env:JAVA_HOME='C:\Program Files\Java\jdk-17'; .\gradlew.bat --no-daemon :app:compileDebugKotlin :app:testDebugUnitTest` passes; the pre-existing `MessagingRepositoryTest`, `ChatPresentationTest`, and `MessagesViewModelTest` stay green; a new unit test `MessagingRepositoryCompanionConversationTest` confirms `ensureConversation(companionContact)` returns a `Conversation` whose marker is populated.

## 3. ViewModel wiring

- [x] 3.1 Extend `feature/chat/ChatRoute.kt` `ChatViewModel` to accept `companionTurnRepository: CompanionTurnRepository` from `AppContainer`. Rewrite `sendMessage(body, attachmentInput)` so it branches on the loaded conversation's companion marker: when the marker is present, call `companionTurnRepository.submitUserTurn(conversationId, body, activeLanguage, parentMessageId)`; otherwise, keep the unchanged `messagingRepository.sendMessage(...)` path so peer-IM flows do not regress. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.chat.ChatViewModelCompanionDispatchTest` asserts both branches using fake repositories — companion branch invokes `submitUserTurn` exactly once and does not touch `messagingRepository.sendMessage`; peer branch invokes `messagingRepository.sendMessage` exactly once and does not touch `companionTurnRepository`.

- [x] 3.2 Extend `ChatUiState` to carry the companion turn state flow and surface `List<ChatMessage>` whose lifecycle statuses are sourced from `companionTurnRepository.observeTurnState(...)` (or the equivalent API confirmed in §1.1) when the conversation is companion-type. For peer conversations the existing `messagingRepository.conversation(...)`-sourced messages stay the source. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.chat.ChatViewModelUiStateCompanionTest` asserts Pending → Thinking → Streaming → Completed transitions reach `uiState.messages` in order and the rendered `ChatMessage.status` carries the lifecycle value at each step.

## 4. Activate handler

- [x] 4.1 Update `feature/tavern/CharacterDetailRoute.kt` `onActivate` so it constructs a companion conversation (carrying the marker from §2.1) before invoking `navController.navigate("chat/${conversation.id}")`. The handler must keep calling `container.companionRosterRepository.activateCharacter(card.id)` as today — only the conversation-construction call changes. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.tavern.CharacterDetailActivateCompanionConversationTest` asserts the conversation returned to the navController carries the companion marker and the card id matches the activated card.

## 5. End-to-end instrumentation

- [ ] 5.1 Add `android/app/src/androidTest/java/com/gkim/im/android/feature/chat/CompanionChatEndToEndInstrumentationTest.kt` that launches the real `GkimRootApp` with a test `AppContainer` whose `CompanionTurnRepository` is a fake emitting a scripted Thinking → Streaming → Completed sequence. The test navigates tavern → character detail → activate → chat, types `"hello"` into the composer, taps Send, and asserts: (a) an outgoing user bubble renders at `MessageStatus.Pending` within 1 second of the tap, (b) a companion Thinking bubble appears within 2 seconds, (c) a Streaming bubble replaces the Thinking bubble within the scripted window, (d) a Completed bubble with the scripted body renders by the end of the scripted sequence. Verification: `cd /x/Repos/GKIM/android && ANDROID_SDK_ROOT=/d/android/Sdk JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.gkim.im.android.feature.chat.CompanionChatEndToEndInstrumentationTest` passes on `codex_api34(AVD) - 14`.

- [ ] 5.2 Extend `CompanionChatEndToEndInstrumentationTest` with a Failed-terminal scenario: scripted Thinking → Failed carrying a retryable `FailedSubtype` (e.g., `Transient`). The test asserts a Failed-labelled companion bubble renders with per-subtype copy and the user bubble exposes a Retry affordance. Verification: same gradlew invocation passes both scenarios; instrumentation log lists two tests run.

## 6. Spec delta

- [ ] 6.1 Finalize `openspec/changes/wire-companion-turn-runtime/specs/core/im-app/spec.md` with the ADDED Requirement *"Companion chat Send path invokes CompanionTurnRepository end-to-end"* covering (a) submit-path dispatch, (b) optimistic Pending user bubble on submit, (c) `ChatMessageRow` rendering from the companion state flow, and (d) end-to-end instrumentation on `codex_api34`. Verification: `openspec validate wire-companion-turn-runtime --strict` passes.

## 7. Verification and delivery evidence

- [ ] 7.1 Run the full unit + instrumentation suites and confirm green: `$env:JAVA_HOME='C:\Program Files\Java\jdk-17'; .\gradlew.bat --no-daemon :app:testDebugUnitTest` and `cd /x/Repos/GKIM/android && ANDROID_SDK_ROOT=/d/android/Sdk JAVA_HOME='/c/Program Files/Java/jdk-17' ./gradlew.bat --no-daemon :app:connectedDebugAndroidTest`. Verification: both invocations return BUILD SUCCESSFUL and the new `CompanionChatEndToEndInstrumentationTest` is listed in the connectedDebugAndroidTest run summary.

- [ ] 7.2 Record verification, review, score (≥95), and GitHub upload evidence in `docs/DELIVERY_WORKFLOW.md` under a new `wire-companion-turn-runtime delivery evidence` section following prior slices' template. Include: (a) each task row 1.1 through 7.1 with commit SHA + push remote (`origin/feature/ai-companion-im`), (b) the `CompanionChatEndToEndInstrumentationTest` connectedDebugAndroidTest output lines showing the run on `codex_api34(AVD) - 14`, and (c) the `openspec archive wire-companion-turn-runtime --yes` command + its stdout. Verification: `docs/DELIVERY_WORKFLOW.md` carries the new section with the rows as listed; `openspec validate wire-companion-turn-runtime --strict` passes after the append.
