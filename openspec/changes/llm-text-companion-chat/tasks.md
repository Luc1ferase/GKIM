## 1. Contract foundations

- [x] 1.1 Extend `android/app/src/main/java/com/gkim/im/android/core/model/ChatModels.kt` with `MessageStatus` (`Pending`, `Thinking`, `Streaming`, `Completed`, `Failed`, `Blocked`, `Timeout`), `CompanionTurnMeta` (turnId, variantGroupId, variantIndex, providerId, model, isEditable, canRegenerate), and optional `parentMessageId: String?` + `status: MessageStatus` + `companionTurnMeta: CompanionTurnMeta?` on `ChatMessage`. New fields default so peer-IM call sites remain source-compatible. Verification: `$env:JAVA_HOME='C:\Program Files\Java\jdk-17'; .\gradlew.bat --no-daemon :app:compileDebugKotlin :app:testDebugUnitTest` passes and existing tests (`ChatPresentationTest`, `RepositoriesTest`, `MessagesViewModelTest`) remain green.

- [x] 1.2 Extend `android/app/src/main/java/com/gkim/im/android/data/remote/im/ImBackendModels.kt` with `CompanionTurnSubmitRequestDto`, `CompanionTurnRecordDto`, `CompanionTurnPendingListDto`, and add `ImGatewayEvent.CompanionTurnStarted/Delta/Completed/Failed/Blocked` cases plus `ImGatewayEventParser` wiring for `companion_turn.started` / `companion_turn.delta` / `companion_turn.completed` / `companion_turn.failed` / `companion_turn.blocked`. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.ImBackendPayloadsTest` passes new round-trip + parser tests for every event type.

- [x] 1.3 Extend `android/app/src/main/java/com/gkim/im/android/data/remote/im/ImBackendClient.kt` with `submitCompanionTurn(baseUrl, token, request)`, `regenerateCompanionTurn(baseUrl, token, turnId, clientTurnId)`, `listPendingCompanionTurns(baseUrl, token)`, and `snapshotCompanionTurn(baseUrl, token, turnId)`. Add matching default stubs that `error("not implemented")` for backward-compat. Implement them in `ImBackendHttpClient`. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.ImBackendHttpClientTest` covers success + error paths for each new endpoint.

- [x] 1.4 Finalize `openspec/changes/llm-text-companion-chat/specs/im-backend/spec.md` so HTTP endpoints, WS event shapes, variant-tree persistence, pending-turn recovery, server-side persona prompt assembly, `{{user}}` substitution, and language steering are all captured. Verification: `openspec validate llm-text-companion-chat --strict` passes.

## 2. Companion turn repository (Android)

- [x] 2.1 Add `android/app/src/main/java/com/gkim/im/android/data/repository/CompanionTurnRepository.kt` with interface `CompanionTurnRepository`, data types for variant-tree state, and `DefaultCompanionTurnRepository` maintaining invariants: one user message per submit; siblings under shared `variantGroupId`; append-only regenerate; active-path navigation via `selectVariant(turnId, variantIndex)`. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.repository.CompanionTurnRepositoryTest` covers submit → thinking → streaming → completed, regenerate appends sibling, swipe navigation, blocked/failed/timeout terminal transitions, and reducer idempotency under duplicate deltas.

- [x] 2.2 Add `android/app/src/main/java/com/gkim/im/android/data/repository/LiveCompanionTurnRepository.kt` that wires the default repository to `ImBackendClient` (HTTP submit / regenerate / snapshot / pending) and `RealtimeGateway.events` (subscribing to companion_turn.* events). On startup/reconnect call `listPendingCompanionTurns` and rehydrate per-turn state. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.repository.LiveCompanionTurnRepositoryTest` uses fakes to assert pending rehydration, event-driven state reducer, and fallback to snapshot on delta gap (monotonic `deltaSeq` violation).

- [ ] 2.3 Register `companionTurnRepository` in `AppContainer` + `DefaultAppContainer` and expose it to the shared chat view model path. Do not alter the peer-IM code path. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.data.repository.*Test` stays green; `rg -n "companionTurnRepository" android/app/src/main/java` confirms wiring.

## 3. Android chat UI

- [ ] 3.1 Extend `android/app/src/main/java/com/gkim/im/android/feature/chat/ChatRoute.kt` `ChatMessageRow` rendering to cover companion lifecycle states: `Thinking` shimmer bubble with persona avatar, `Streaming` growing text with pulse indicator, `Completed` final text with model + regenerate affordance on the most recent companion variant, `Failed` with Retry action + reason copy, `Blocked` with neutral copy citing the block reason, `Timeout` as a distinct subtype of `Failed` (explicit wording). Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.chat.ChatPresentationTest` covers each state + transitions.

- [ ] 3.2 Add first-message / alternate-greeting picker UI: when the user enters a companion conversation whose message history is empty, present a bottom-sheet or dedicated panel listing the active card's resolved `firstMes` plus each `alternateGreetings` entry in the active `AppLanguage`. Selecting a greeting submits it as a companion variant at `variantIndex=0` via the repository. Once any companion message exists, the picker does not render. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.chat.*PickerTest` plus an instrumentation test asserting picker-to-bubble flow on `codex_api34`.

- [ ] 3.3 Add swipe controls + regenerate action on companion bubbles: small inline chevrons with "n/m" indicator on active-variant companion bubbles; tapping chevrons calls `selectVariant`; tapping the regenerate pill on the most recent companion variant calls `regenerateCompanionTurn` and immediately sets the new turn to `Thinking`. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.chat.ChatVariantInteractionTest` + instrumentation `regenerateAppendsVariantAndKeepsHistory` on `codex_api34`.

- [ ] 3.4 Wire submit path: when the user taps Send in a companion conversation, call `CompanionTurnRepository.submitUserTurn(conversationId, body, activeLanguage, parentMessageId)`. On network/server failure surface the failure on the user message bubble (not the companion bubble) so retry semantics are clear. Verification: unit test covers submit-failure surface + retry; instrumentation `companionConversationSendShowsThinkingThenStreaming` on `codex_api34`.

## 4. Backend contract alignment

- [ ] 4.1 Finalize `openspec/changes/llm-text-companion-chat/specs/im-backend/spec.md` contract around HTTP endpoints, WS events, variant persistence, `{{user}}` substitution, language steering, pending recovery, and safety block reasons. Record backend migration intent (new `companion_turns` + `companion_turn_variants` tables or equivalent, monotonic `deltaSeq`, pending turn index) in this slice's design.md without committing Rust source. Verification: `openspec validate llm-text-companion-chat --strict` passes and maintainer handoff note in the delivery record points to the private backend PR.

- [ ] 4.2 Document the provider abstraction expectations: first-slice backend must accept at least one OpenAI-compatible text provider; additional providers (Tongyi Qwen text, Hunyuan text) are optional in this slice but the contract must not bake in a single vendor. Record in design.md; no public code change. Verification: design.md § "Provider abstraction" exists and is referenced from the delivery record.

## 5. Verification and delivery evidence

- [ ] 5.1 Add focused unit suites for reducer + navigation: `CompanionTurnRepositoryTest`, `LiveCompanionTurnRepositoryTest`, `ImBackendPayloadsTest` (companion_turn events), `ImBackendHttpClientTest` (new endpoints), plus update `ChatPresentationTest` and `MessagesViewModelTest` as needed. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest` fully green.

- [ ] 5.2 Add instrumentation coverage on `codex_api34`: companion conversation entry with picker, streaming bubble render, regenerate appends + swipe navigates, blocked/failed/timeout bubble rendering, pending-turn recovery after kill-restart mid-Streaming. Verification: `.\gradlew.bat --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.gkim.im.android.feature.chat.LlmCompanionChatTest` passes.

- [ ] 5.3 Record verification, review, score (≥95), and GitHub upload evidence in `docs/DELIVERY_WORKFLOW.md` for this slice. Verification: the delivery workflow entry exists with task rows 1.1 through 5.2 plus this recording task.
