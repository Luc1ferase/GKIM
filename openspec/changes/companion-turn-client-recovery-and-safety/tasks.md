# Tasks — companion-turn-client-recovery-and-safety

## §0 — OpenSpec scaffold

- [x] §0.1 — `proposal.md` / `tasks.md` / `specs/core/im-app/spec.md` + `specs/llm-text-companion-chat/spec.md` deltas committed on branch `feature/companion-turn-client-recovery-and-safety` off `feature/ai-companion-im`. Scaffold commit `8d9b257`, pushed to `origin/feature/companion-turn-client-recovery-and-safety`. `openspec validate companion-turn-client-recovery-and-safety --strict` returns valid.

## §1 — Three UX papercuts (the user-flagged trio from 2026-04-27)

- [x] §1.1 — Remove the legacy English-only "Regenerate" affordance on the most-recent companion bubble in `feature/chat/ChatRoute.kt`. Concretely: delete the `if (companionPresentation.showRegenerate) { Text(text = "Regenerate", ...) }` block (was lines 1121-1130, drifted from tasks.md's 1152-1161) plus the `onRegenerate: () -> Unit = {}` parameter on `ChatMessageRow` (was line 932) since no caller binds it. The bilingual "Regenerate from here / 从这里重新生成" Text remains as the single regenerate affordance for ALL companion bubbles. The `showRegenerate` boolean on `CompanionLifecyclePresentation` is RETAINED — multiple existing presentation tests assert it's false on Failed/Blocked/Timeout bubbles. Drive-by: removed the now-orphaned `chat-companion-regenerate-${message.id}` testTag assertion from `ChatFailureAndSafetyBubbleInstrumentationTest`. Evidence: commit `33c712a` on `feature/companion-turn-client-recovery-and-safety`, pushed to origin. `:app:compileDebugKotlin` BUILD SUCCESSFUL; `:app:testDebugUnitTest --tests ChatPresentationTest --tests ChatBlockedBubbleTest --tests ChatFailedBubbleTest --tests ChatTimeoutBubbleTest` BUILD SUCCESSFUL.

- [x] §1.2 — Add a "Settings / 设置" entry to the `ChatTopBar` overflow dropdown in `ChatRoute.kt::ChatTopBar` (after the existing "Export chat / 导出对话" `DropdownMenuItem`). Threading: a new `onOpenSettings: (() -> Unit)?` parameter on `ChatTopBar` + `ChatScreen`, bound in `ChatRoute` to `navController.navigate("settings")` when `uiState.conversation?.companionCardId != null` (companion-conversation gate). Bilingual copy via `LocalAppLanguage`. `ChatTopBar` visibility raised from `private` to `internal` so the new instrumentation test can drive it. Evidence: commit `81aa5f5` on `feature/companion-turn-client-recovery-and-safety`, pushed to origin. New `ChatTopBarOverflowInstrumentationTest` with two cases (English + Chinese) asserts both items are findable by their `testTag` and that tapping Settings invokes `onOpenSettings`. `:app:compileDebugKotlin`, `:app:compileDebugAndroidTestKotlin`, and `:app:testDebugUnitTest --tests "*chat.*"` all BUILD SUCCESSFUL.

- [x] §1.3 — Drop the `"Ready"` literal from the Completed lifecycle's `statusLine` fallback (was at `ChatRoute.kt:1480`, drifted from tasks.md's 1499). Concretely: changed `CompanionLifecyclePresentation.statusLine` from `String` to `String?`, set the Completed branch to `statusLine = modelBadge?.let { "Model · $it" }` (no fallback string), and wrapped the render-side `Text` in `companionPresentation.statusLine?.let { line -> Text(text = line, ...) }` so a null status line skips the row entirely. Adjusted two `.statusLine.startsWith(...)` callsites in `ChatPresentationTest.kt:168` and `ChatTimeoutBubbleTest.kt:122` to `?.startsWith(...) == true` for the nullable type. Added new `ChatPresentationTest` case `companion lifecycle completed state hides status line when no model badge is available` asserting `statusLine == null` when `model = null`. The existing `Model · gpt-4o-mini` case is preserved unchanged. Evidence: commit `1b69f02` on `feature/companion-turn-client-recovery-and-safety`, pushed to origin. `:app:compileDebugKotlin`, `:app:compileDebugUnitTestKotlin`, the four targeted presentation tests, and the full `:app:testDebugUnitTest` sweep all BUILD SUCCESSFUL.

## §2 — Backend wire-shape verification (paired with `companion-turn-backend-recovery-and-safety`)

- [ ] §2.1 — Mirror three new event fixtures byte-equivalent from the backend repo (paired-PR convention): `contract/fixtures/companion-turns/event-failed.json`, `event-blocked.json`, `event-timeout.json`. Each fixture covers one terminal variant of the wire shape. Verification: `git diff --no-index contract/fixtures/companion-turns/event-{failed,blocked,timeout}.json ../GKIM-Backend/contract/fixtures/companion-turns/event-{failed,blocked,timeout}.json` returns empty after the mirror.

- [ ] §2.2 — Add three round-trip serde tests asserting each new fixture deserializes into the existing `ImGatewayEvent.CompanionTurnFailed { turnId, conversationId, messageId, subtype, errorMessage, completedAt }`, `.CompanionTurnBlocked { turnId, conversationId, messageId, reason, completedAt }`, `.CompanionTurnTimeout { turnId, conversationId, messageId, elapsedMs, completedAt }` variants. Author the tests in `data/remote/im/CompanionTurnEventSerializationTest.kt` if one exists, otherwise create a new file. Each test (a) reads the fixture from the resource path, (b) parses via the project's `Json` instance, (c) asserts the resulting event type is the right variant and the field values match. Verification: `:app:testDebugUnitTest --tests "*.CompanionTurnEventSerializationTest"` BUILD SUCCESSFUL with the three new test methods green.

- [ ] §2.3 — Verify the `CompanionTurnRepository` gateway-event handler converts each parsed event into the right `MessageStatus`: `companion_turn.failed` → `MessageStatus.Failed` with `failedSubtypeKey` populated; `.blocked` → `MessageStatus.Blocked`; `.timeout` → `MessageStatus.Timeout`. The handler code already exists per `data/repository/CompanionTurnRepository.kt:399-401`; this task adds end-to-end assertions that exercise the full parse → handler → ChatMessage flow with stub gateway events. Verification: `:app:testDebugUnitTest --tests "*.CompanionTurnRepositoryRecoveryEventTest"` BUILD SUCCESSFUL with three new test methods (one per terminal kind).

## §3 — Verification + archive

- [ ] §3.1 — Verification roll-up: §1-§2 evidence rows recorded in `docs/DELIVERY_WORKFLOW.md` under `companion-turn-client-recovery-and-safety delivery evidence`. All new unit tests green; full `:app:testDebugUnitTest` sweep BUILD SUCCESSFUL with no regressions. Paired backend slice `companion-turn-backend-recovery-and-safety` exists at the SHA recorded by its §0.1; the byte-equivalent fixture mirror is asserted at §2.1.

- [ ] §3.2 — Archive — move `openspec/changes/companion-turn-client-recovery-and-safety/` → `openspec/changes/archive/<date>-companion-turn-client-recovery-and-safety/` and apply the spec deltas to `openspec/specs/core/im-app/spec.md` + `openspec/specs/llm-text-companion-chat/spec.md` (1 MODIFIED Requirement each, with Scenarios added). Verification: `openspec archive companion-turn-client-recovery-and-safety --yes` reports the modifications applied; post-archive `openspec validate --specs --strict` returns clean.
