## 1. Contract foundations

- [x] 1.1 Add `android/app/src/main/java/com/gkim/im/android/core/model/UserPersonaModels.kt` with `UserPersona` (`id`, `displayName: LocalizedText`, `description: LocalizedText`, `isBuiltIn`, `isActive`, `createdAt`, `updatedAt`, `extensions: JsonObject`). Include a `UserPersonaValidation` helper that rejects blank display names or blank descriptions on either language side. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:compileDebugKotlin :app:testDebugUnitTest --tests com.gkim.im.android.core.model.UserPersonaModelsTest` - pass (8 cases: data-class equality considers every field, `resolve()` returns active-language strings with `isActive` / `isBuiltIn` propagated, `isDeletable` requires user-owned and inactive, `extensions` JsonObject survives `copy()`, validation accepts complete bilingual persona, rejects blank English display name with `DisplayNameEnglishBlank` code, rejects blank Chinese description with `DescriptionChineseBlank` code, and reports all four blank sides simultaneously when display name and description are entirely blank). `UserPersonaValidation.validate(persona)` returns sealed `UserPersonaValidationResult.Valid` or `Invalid(errors: List<UserPersonaValidationError>)` covering the four blank-side codes.

- [x] 1.2 Add `android/app/src/main/java/com/gkim/im/android/core/model/MacroSubstitution.kt` exposing `substituteMacros(template: String, userDisplayName: String, charDisplayName: String): String`. It MUST handle the six forms `{{user}}`, `{user}`, `<user>`, `{{char}}`, `{char}`, `<char>`, and leave unknown macros untouched. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.core.model.MacroSubstitutionTest` - pass (13 cases: each of the six forms resolves individually (`{{user}}` / `{user}` / `<user>` / `{{char}}` / `{char}` / `<char>`), mixed user+char forms in one template, case-insensitive matching across all six forms (`{{USER}}` / `{{User}}` / `<CHAR>` etc.), unknown macros like `{{random}}` / `{foo}` / `<bar>` / whitespaced `{{ user }}` stay untouched, empty user name leaves user macros raw while char macros still resolve, empty char name leaves char macros raw while user macros still resolve, both empty leaves the template untouched, no-macro template untouched, repeated forms all substitute, and `UserForms` / `CharForms` expose the exact six forms as the canonical list). The helper uses case-insensitive `String.replace(..., ignoreCase = true)` so imported ST cards with capitalised macros (`{{User}}`) still substitute the active persona / companion display name.

- [x] 1.3 Extend `android/app/src/main/java/com/gkim/im/android/data/remote/im/ImBackendModels.kt` with DTOs: `UserPersonaDto`, `UserPersonaListDto`, `UserPersonaActivateRequestDto`. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.ImBackendPayloadsTest` - pass (4 new persona cases on top of the existing 16: `UserPersonaDto` decodes bilingual `displayName` + `description` + `isBuiltIn` / `isActive` / `createdAt` / `updatedAt` + `extensions` JsonObject passthrough and re-encodes to equal JSON; `toUserPersona()` converts to domain `UserPersona` with active flag preserved; `UserPersonaListDto` round-trips a two-persona list with `activePersonaId`; `UserPersonaActivateRequestDto` round-trips `personaId`; `UserPersonaDto.fromUserPersona(domain)` → encode → decode → `toUserPersona()` returns a domain value equal to the original including the extensions bag). The DTOs reuse the existing `LocalizedTextDto` for bilingual fields so the persona layer stays consistent with the companion card layer.

- [x] 1.4 Extend `android/app/src/main/java/com/gkim/im/android/data/remote/im/ImBackendClient.kt` with `listPersonas(baseUrl, token)`, `createPersona(...)`, `updatePersona(...)`, `deletePersona(...)`, `activatePersona(baseUrl, token, personaId)`, and `getActivePersona(baseUrl, token)`. Implement in `ImBackendHttpClient` with default `error("not implemented")` stubs for backward-compat. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.data.remote.im.ImBackendHttpClientTest` - pass (30 cases in `ImBackendHttpClientTest` = 18 existing + 12 new persona cases: `listPersonas` attaches bearer token and decodes `UserPersonaListDto` + raises on 404; `createPersona` POSTs body to `/api/personas` and decodes the stored persona + raises on 409 duplicate conflict; `updatePersona` POSTs body to `/api/personas/{personaId}` and returns updated record + raises on 404 for missing persona; `deletePersona` POSTs to `/api/personas/{personaId}/delete` and attaches bearer token + raises on 409 when server blocks deleting the active persona (the spec's delete-active-blocked path); `activatePersona` POSTs to `/api/personas/{personaId}/activate` and returns the new active persona with `isActive=true` + raises on 404 unknown persona; `getActivePersona` GETs `/api/personas/active` and returns the built-in default persona + raises on 404 when no active persona is set). The 6 endpoints are declared on `ImBackendClient` with `error("<name> is not implemented for this backend client")` default stubs (lines 120-154 of ImBackendClient.kt) so legacy in-process clients remain binary-compatible while `ImBackendHttpClient` overrides all six against the Retrofit service.

- [x] 1.5 Finalize `openspec/changes/user-persona/specs/im-backend/spec.md` so persistence, CRUD, active-singleton enforcement, built-in seeding, macro substitution semantics, and the allocator integration slot are captured as requirements with scenarios. Verification: `npx --yes openspec validate user-persona --strict` - pass (`Change 'user-persona' is valid`). The im-backend delta now contains 5 ADDED requirements: (a) persistence + CRUD HTTP endpoints with a 3-scenario block covering list, round-trip CRUD, and reconnect/restart durability; (b) exactly-one-active enforcement with 4 scenarios covering atomic exclusive activation, bootstrap exposing the active id, rejecting built-in deletion, and rejecting active deletion; (c) built-in seeding on first bootstrap with 2 scenarios covering initial seed and no-duplicate on re-bootstrap; (d) macro substitution for all six forms (`{{user}}` / `{user}` / `<user>` / `{{char}}` / `{char}` / `<char>`) with 3 scenarios covering substitution in assembled prompts, stored bodies retaining raw macros, and unknown-macro passthrough; and (e) allocator integration with 3 scenarios covering inclusion when budget allows, drop order between rolling summary and non-critical preset sections, and `{{user}}` substitution surviving description drop. The delta plus `core/im-app/spec.md` and `user-persona/spec.md` deltas collectively pass `--strict` validation.

## 2. UserPersona repository (Android)

- [x] 2.1 Add `android/app/src/main/java/com/gkim/im/android/data/repository/UserPersonaRepository.kt` interface exposing `observePersonas(): Flow<List<UserPersona>>`, `observeActivePersona(): Flow<UserPersona?>`, `create(persona)`, `update(persona)`, `delete(personaId)`, `activate(personaId)`, `duplicate(personaId)`, and `refresh()`. `DefaultUserPersonaRepository` enforces invariants: exactly one persona has `isActive=true`; the built-in persona cannot be deleted; duplicate produces a user-owned persona with bilingual `" (copy)"` suffix. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.data.repository.UserPersonaRepositoryTest` - pass (11 cases: activate flips the active flag exclusively across the library (asserts exactly one active survives and previous active flips to inactive); activate unknown persona returns `Rejected(UnknownPersona)`; delete of built-in persona returns `Rejected(BuiltInPersonaImmutable)` and does not remove the record; delete of currently active persona returns `Rejected(ActivePersonaNotDeletable)` and preserves library size; delete of inactive user-owned persona succeeds and removes it from the library; duplicate produces a user-owned persona with bilingual `" (copy)"` / `"（副本）"` suffix and `isBuiltIn=false` + `isActive=false` + a fresh generated id; duplicate of unknown persona returns `Rejected(UnknownPersona)`; create normalizes a new persona with a generated id, `isBuiltIn=false`, `isActive=false`, and both timestamps set to the clock value even when the input claims built-in/active or a stale `createdAt`; update preserves `isBuiltIn`, `isActive`, and `createdAt` from the existing record while rewriting `updatedAt` to the clock value; `observeActivePersona` emits null when no persona is active and the active persona after activate; ingesting a snapshot with multiple `isActive=true` personas collapses to exactly one active via `enforceSingleActive`). The mutation result is `UserPersonaMutationResult.Success(persona)` or `Rejected(reason: RejectionReason)` with `RejectionReason { UnknownPersona, BuiltInPersonaImmutable, ActivePersonaNotDeletable }`.

- [x] 2.2 Add `android/app/src/main/java/com/gkim/im/android/data/repository/LiveUserPersonaRepository.kt` that binds the default repository to `ImBackendClient` endpoints. On cold start `refresh()` loads the library and active-persona in parallel and merges them. Optimistic local update is rolled back on 4xx/5xx. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.data.repository.LiveUserPersonaRepositoryTest` - pass (12 cases: (merge semantics) `refresh` merges remote list with the `activePersonaId` from the bootstrap response so the single active flag is applied consistently across the library; `refresh` also reconciles when `activePersonaId=null` on the list but `getActivePersona` returns a record - the `async { listPersonas } / async { getActivePersona }` parallel fetch is merged via `mergeRemote(...)`; `refresh` is a no-op when session has no base url / token; (activate-from-list) activate flips the active flag locally AND reaches the backend with the new active record id, and the server-returned persona replaces the local active entry (`lastActivateId` records the id forwarded to the mock backend); (rollback on 409/4xx/5xx) activate rolls back the local active flag to the previous active persona when the backend raises 409; delete rolls back the local removal when the backend raises 409 for active-persona deletion; delete short-circuits on a local `Rejected(BuiltInPersonaImmutable)` without reaching the backend (asserted via `deleteCalls==0`); create rolls back the optimistic insert when the backend raises 500; create reconciles the local record with the server-returned persona id on success; duplicate rolls back the optimistic copy when the backend create fails; update rolls back the local description change when the backend raises 422; mutations without session (null baseUrl) short-circuit to local-only Success without calling the backend. Backend failure surfaces as `UserPersonaMutationResult.Failed(cause)` (new case added to the sealed interface) so callers can distinguish invariant-rejection from network failure.

- [x] 2.3 Register `userPersonaRepository` in `AppContainer` + `DefaultAppContainer` and expose it to the chat + settings view models that need active-persona state. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests "com.gkim.im.android.data.repository.*Test"` - pass (all repository tests green after wiring `userPersonaRepository` into the `AppContainer` interface and `DefaultAppContainer` via `LiveUserPersonaRepository(default = DefaultUserPersonaRepository(initialPersonas = seedBuiltInPersonas), backendClient = imBackendClient, baseUrlProvider = { sessionStore.baseUrl }, tokenProvider = { sessionStore.token })`). The three instrumentation `AppContainer` impls (`UiTestAppContainer` in `GkimRootAppTest`, `LiveImageValidationContainer` in `LiveImageSendValidationTest`, and `LoginEndpointTestAppContainer` in `LoginEndpointConfigurationTest`) each gain a `DefaultUserPersonaRepository(initialPersonas = seedBuiltInPersonas)` override, keeping the instrumentation fakes compilable without the HTTP live binding. `seedBuiltInPersonas` in `SeedData.kt` now exposes the single built-in persona `persona-builtin-default` with `isBuiltIn=true`, `isActive=true`, bilingual `displayName = LocalizedText("You", "你")`, and a neutral bilingual description - matching the backend-seed contract in the im-backend spec delta so cold starts without a session still expose one active persona. `grep -c "userPersonaRepository"` reports matches in all four container files (main: 2, each instrumentation: 1).

## 3. Android Settings: persona library

- [x] 3.1 Add a "Personas" section to Settings (in the existing Settings route or a sibling route) listing every persona from `UserPersonaRepository.observePersonas()`, showing the active persona with a visible badge, and exposing entry points for "New persona", "Duplicate", "Edit", "Activate", and "Delete" (disabled for built-ins and the currently active persona). Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.feature.settings.PersonaListPresentationTest` - pass (9 cases: list ordering places built-ins first then user personas each ordered by `createdAt` ascending; active persona flag is carried into `PersonaListItem.isActive` and surfaces as the active badge; built-in personas cannot be deleted even when inactive (`canDelete=false` for both active and inactive built-ins; `canDelete=true` only for inactive user-owned personas); active persona cannot be deleted or activated again but remains editable and duplicable (`canDelete=false`, `canActivate=false`, `canEdit=true`, `canDuplicate=true`) while inactive built-ins show `canActivate=true` + `canDelete=false`; activate flips the active persona exclusively and clears `pendingOperation` with no errorMessage on success; resolved persona fields honour the language provider (English renderer emits English display name; Chinese renderer emits Chinese); delete on active persona surfaces the `"Active persona cannot be deleted"` rejection errorMessage and keeps the library intact; failed mutation (`UserPersonaMutationResult.Failed(IllegalStateException("server_busy"))`) surfaces `"server_busy"` as `errorMessage` and is dismissible via `clearError()`; init block triggers `repository.refresh()` exactly once and bumps `refreshCompletions` to 1). The `PersonaLibraryViewModel` projects every persona into a `PersonaListItem { persona, resolved, isActive, canDelete=!isBuiltIn && !isActive, canActivate=!isActive, canEdit=true, canDuplicate=true }`, sorts built-ins before user-owned (each by `createdAt` asc), and exposes commands `activate(id)` / `delete(id)` / `duplicate(id)` / `clearError()` that manage a `PersonaLibraryUiState { items, pendingOperation: PendingOperation?, errorMessage: String? }` with `PendingOperation { personaId, kind: Kind { Activate, Delete, Duplicate } }` for per-row spinners. Settings route gains a `SettingsDestination.Personas` entry (menu tag `settings-menu-personas`, detail tag `settings-detail-personas`); active personas render an `"ACTIVE"` / `"已启用"` badge pill (testTag `settings-personas-active-<id>`), built-ins render a `"BUILT-IN"` / `"内置"` label, and each card exposes Activate / Edit / Duplicate / Delete buttons with `enabled` driven by the matching `canActivate` / `canEdit` / `canDuplicate` / `canDelete` flag plus a `pendingOperation`-aware gate so in-flight mutations short-circuit further clicks. The "New persona" button (testTag `settings-personas-new`) and the Edit button (`settings-personas-edit-<id>`) are wired as stubs awaiting task 3.2's `PersonaEditorRoute`.

- [x] 3.2 Add `android/app/src/main/java/com/gkim/im/android/feature/settings/PersonaEditorRoute.kt` backed by a `PersonaEditorViewModel` that edits bilingual `displayName` and bilingual `description`. Save calls `UserPersonaRepository.update`; cancel discards. Validation rejects blank fields on either language side, using `UserPersonaValidation`. Verification: `JAVA_HOME='/c/Program Files/Java/jdk-17' ./android/gradlew.bat --no-daemon -p android :app:testDebugUnitTest --tests com.gkim.im.android.feature.settings.PersonaEditorPresentationTest` - pass (7 cases: loads existing persona into editor state with bilingual fields + `isBuiltIn` / `isActive` flags propagated and `canSave=false` when no changes have been made; validation surfaces `DisplayNameEnglishBlank` when English display name is blanked and blocks save, then recovers when the field is non-blank again and changes still track; validation surfaces every blank side simultaneously (`DisplayNameEnglishBlank` + `DisplayNameChineseBlank` + `DescriptionEnglishBlank` + `DescriptionChineseBlank`) and blocks save; save success calls `UserPersonaRepository.update` with the current fields, marks `isSaving=false`, populates `savedPersona` with the repository's normalized record, updates the baseline snapshot so `canSave` flips back to false and `hasUnsavedChanges=false`, and the persisted record in `observePersonas()` reflects the new description; cancel restores the loaded snapshot (bilingual display name + description) and flips `hasUnsavedChanges` + `canSave` back to false; unknown persona id surfaces `"Persona not found"` saveError and keeps `canSave=false`; built-in persona cannot be saved even with valid fields (`canSave=false` even after a change)). `PersonaEditorViewModel` loads the persona via `repository.observePersonas().first().firstOrNull { it.id == personaId }`, projects each edit through `recomputeDerived` which re-runs `UserPersonaValidation.validate(persona)` and recomputes `canSave = personaId != null && errors.isEmpty() && hasUnsavedChanges && !isBuiltIn`, and translates `UserPersonaMutationResult.Rejected` / `Failed` into human-readable `saveError` strings. The companion `PersonaEditorRoute` composable renders bilingual `OutlinedTextField`s for display-name English / display-name Chinese / description English / description Chinese (each with its own testTag and `isError` flag driven by the matching validation code), a built-in notice card, an error card with Dismiss button, Cancel / Save action row, and auto-pops back via `LaunchedEffect(savedPersona)` on a successful save. Settings route gains `SettingsDestination.PersonaEditor` + an `editingPersonaId: String?` state that the Personas list's Edit button populates, and the route falls back to `SideEffect { onPersonaEditorDone() }` when the destination is reached without an editing id so back-stack recovery stays robust.

- [ ] 3.3 Add instrumentation `PersonaLibraryInstrumentationTest` on `codex_api34` covering: open Settings → Personas, see the built-in persona marked active, edit its description and confirm the change persists, create a new user-owned persona, activate it, observe the active badge moves, attempt to delete the active persona and observe the action disabled, delete an inactive user persona. Verification: `.\gradlew.bat --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.gkim.im.android.feature.settings.PersonaLibraryInstrumentationTest` passes.

## 4. Chat surface integration

- [ ] 4.1 Extend the companion chat chrome with an active-persona pill next to the active-preset pill. Tapping the persona pill routes to Settings → Personas. The pill's label resolves to the active persona's `displayName` in the active `AppLanguage`. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.chat.ChatChromePersonaPillTest` covers label selection, tap routing, and update on active-persona change.

- [ ] 4.2 Wire the greeting picker and any chat preview renderer that shows macros to use `MacroSubstitution.substituteMacros` with the active persona's display name and the active companion card's display name. The stored greeting body is NOT rewritten; only the rendered preview is substituted. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.chat.GreetingPickerMacroSubstitutionTest` asserts preview text reflects substitution while the stored variant body remains raw.

- [ ] 4.3 Add a subtle footer line under the chrome pills showing "Talking as {personaName}" in the active `AppLanguage`, with typography that does not compete with the conversation content. Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.gkim.im.android.feature.chat.ChatChromePersonaFooterTest` asserts rendering and accessibility semantics (content description).

## 5. Backend contract alignment

- [ ] 5.1 Finalize `openspec/changes/user-persona/specs/im-backend/spec.md` to cover: HTTP endpoints for persona library CRUD + activate + active getter, built-in persona seeding on first bootstrap, active-singleton enforcement, macro substitution rules (`{{user}}`/`{user}`/`<user>` → active persona display name; `{{char}}`/`{char}`/`<char>` → active companion display name), and the persona-description section in the token-budget allocator with its priority and drop position. Verification: `openspec validate user-persona --strict` passes.

- [ ] 5.2 Document in design.md § "Persona description injection in the token-budget allocator" the exact priority slot and drop step inserted into the `companion-memory-and-preset` allocator. Also document the `MacroSubstitution` form list (six forms) so the backend and the Android client stay in sync. Verification: design.md references both sections and the spec delta for `companion-memory-and-preset` reflects the added section.

## 6. Verification and delivery evidence

- [ ] 6.1 Add focused unit suites: `UserPersonaModelsTest`, `MacroSubstitutionTest`, `UserPersonaRepositoryTest`, `LiveUserPersonaRepositoryTest`, `PersonaListPresentationTest`, `PersonaEditorPresentationTest`, `ChatChromePersonaPillTest`, `GreetingPickerMacroSubstitutionTest`, `ChatChromePersonaFooterTest`, plus `ImBackendPayloadsTest` (persona DTOs) and `ImBackendHttpClientTest` (new endpoints). Verification: `.\gradlew.bat --no-daemon :app:testDebugUnitTest` fully green.

- [ ] 6.2 Add instrumentation coverage on `codex_api34`: `PersonaLibraryInstrumentationTest` plus a `PersonaIntegrationChatTest` that switches the active persona mid-session and confirms the next turn reflects the new display name in chrome + footer + greeting preview. Verification: `.\gradlew.bat --no-daemon :app:connectedDebugAndroidTest` runs both suites green.

- [ ] 6.3 Record verification, review, score (≥95), and GitHub upload evidence in `docs/DELIVERY_WORKFLOW.md` for this slice, following the template used by `llm-text-companion-chat` and `companion-memory-and-preset`. Include explicit pointers to the new spec delta files and the macro-form table in design.md. Verification: the delivery workflow entry exists with task rows 1.1 through 6.2 plus this recording task, and lists the `openspec archive` command + output.
